最初に "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/AGENTS.md" を読むこと。
対象: "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/show-creative-ideas/shosai-app/"。新規 stage-i18n.zh-Hans.js / stage-i18n.zh-Hant.js、index.html、style.css、stage-sketch.js（言語選択UIのみ）、stage-prompt-i18n.js、tests/。
目的: 実装。docs/I18N_ZH_WORKORDER_2026-09-09.md の §3.2 を、§6 R3 の完了条件まで。
確定仕様: §3.2 の1〜6。訳文はドラフトから一字も変えない。訳語を新しく決めない。
  製品名の表記は §0-b の決定: ② 英名 "Stage Sketch"（ドラフトの 舞台速寫／舞台速写 は出さない。§3.2-6 の②の手順）。
未決事項: §4 の4件は実装せず、報告の末尾に提案として書く。
制約: §5。i18n-prep/ は読むだけ（コピー元）。版上げ（?v= / CACHE_NAME / build_stage.py）はこのラウンドでは**しない**。
完了報告: 変更・新規ファイル／各テストの追加内容と結果／かな漏れ検査の実測（0件の証拠、あれば全件）／
  スクリーンショットの保存先／初回ロード増分の実測（KB・ms）／§4 への提案／ドラフトの NEEDS_REVIEW をそのまま転記。
補足（R2b の実績を引き継ぐ。必ず読む）:
  - R2b で実装済みの前提: window.SHOSAI_I18N_PACKS.en が {text, maps, say, generated}。stage-sketch.js の packValue() が「現在言語のパック → en パック → 日本語原文」の順で引く。
    normalizeStageLanguage() は読み込まれていないパックの言語を en へ落とす（77〜89行）。stage-public.js と stage-set-builder.js は既にパック参照。
  - 編集前に stage-sketch.js / stage-i18n.js / index.html / style.css / stage-prompt-i18n.js を <name>_backup_2026-09-09-before-zh.<ext> の名で複製する。
  - 製品名（§0-b=②英名）: コピーした stage-i18n.zh-Hant.js 内の「舞台速寫」（9箇所）と stage-i18n.zh-Hans.js 内の「舞台速写」（9箇所）を、文中も含めてすべて "Stage Sketch" に置換する
    （中西混植は STYLE どおり前後に空白を入れない）。置換した鍵の一覧を i18n-prep/NEEDS_REVIEW.md に「## 2026-09-09 製品名を英名に置換した鍵」として記録。i18n-prep/ のドラフト自体は触らない。
    これ以外の訳文は一字も変えない（diff で「グローバル名の行」と「製品名の置換」だけが差であることを報告に示す）。
  - 言語選択UIの説明文「画面の文字を日本語と英語で切り替えます。」は鍵も文言も変えない（本人判断待ち。報告の §4 提案欄に「4言語になったので文言更新が要る」と書く）。
  - innerText の基準値（R2b 完了時・ローカルHTTP 127.0.0.1:18431＋隔離した新規 Chrome プロファイル）:
      ?lang=en → d10ed544fa5093bec741ec4490312aa1cf99564928b291374647de66852a6fc6（2474 bytes）
      ?lang=ja → 7063741df129ce2a8192510c638c250228760339ad541a5b6b65623a8d8c3154（3340 bytes）
    言語選択UIをセレクトへ変えると設定モーダル内の文字が変わりうる。基準と差分が出た場合は「セレクトの選択肢文字だけ」であることを差分全件で示す。
  - かな漏れ検査: ?lang=zh-Hant / ?lang=zh-Hans で開き、道具列・設定モーダル・シーン一覧・出るもの・明かり・書き出しの各パネルを開いた状態で document.body.innerText を取り、
    [぀-ヿ] に当たる文字列を data-no-i18n 配下と利用者データを除いて全件列挙（0件が目標。残るなら鍵と理由を表に）。
  - スクリーンショットは docs/i18n-zh-2026-09/ へ（zh-Hant・zh-Hans それぞれ正面図と設定モーダル）。
  - 版上げ（?v= / CACHE_NAME / build_stage.py）はしない。stage.html は生成物なので触らない（検証は index.html を直接開いてよい。stage.html と index.html の関係は build_stage.py 冒頭を読む）。
  - コミットしない。git add -A しない。判断が要る不整合が出たら止めて報告する。

【R3b＝再開の指示。前回（R3）の停止報告への回答】
  推奨案で続行。発注書 §6 R3 を次のとおり読み替えて確定した（本文も更新済み。読み直すこと）:
    1. 中国語ドラフトの TEXT 989鍵・MAPS 20群 256 id・SAY 210文型が、欠落・改変なくパックに登録されていること（評価後の値で照合）。
    2. zh パックの鍵・群・id・SAY source が en パックの集合の部分集合であること（zh にだけある鍵が無い）。
    3. en にあって zh に無い鍵（R1 追加分・MAPS 群 setBuilder）が「現在言語 → en → 日本語」の順で英語に落ちることを、tx / tm / sx / generated それぞれ評価後の値で確認。
    4. 訳文の差分は「グローバル公開行」と「製品名の置換（各9箇所）」だけ。
  その他（引用符の系統違い・訳語の衝突・かな漏れ・スクリーンショット・日英 innerText 基準）は元の §6 R3 のまま。
  作成済みのバックアップ5本はそのまま使う（作り直さない）。R3 を最後まで実装・検証して報告する。

【R3c＝再開の指示。前回（R3b）の停止報告への回答】
  2例外を許可する。訳文は変えない。
    1. `整列（一列・円・V字）` は孤児鍵（アプリ本体から消えた文言。stage-i18n_backup_2026-08-12.js にだけ英訳が残る）。zh 固有鍵の**唯一の許容例外**として、部分集合テストに明示の許可リスト（この1鍵だけ）を持たせる。
       NEEDS_REVIEW.md に「## 2026-09-09 孤児鍵（ドラフトにだけ残る）」として記録。パックからは**削除しない**（ドラフト無変更の原則）。
    2. zh-Hans の MAPS `dimBy.pole.h` と `dimBy.cane.h` の同訳 `杆高` は意図的同訳として、衝突テストに明示の許可リスト（この1組だけ）を持たせる。
       NEEDS_REVIEW.md に「## 2026-09-09 同一群内の同訳（ネイティブ確認へ）」として、zh-Hant が 竿高／桿高 で区別している事実を添えて記録。
  許可リストは「鍵・id をそのまま書いた配列」にし、正規表現や件数での緩和はしない（次に同種が増えたら検知できるように）。
  この2例外で R3 の残り（全テスト・かな漏れ検査・日英 innerText 基準比較・ブラウザ表示・スクリーンショット・ロード増分の実測・§4 提案）を最後まで実施し、§7.3 の完了報告の形式で報告する。

【R3d＝再開の指示。前回（R3c）の停止報告への回答】
  推奨案で続行。
    1. MAPS.venueNote の5 id（proscenium / thrust / arena / outdoor / blackbox）を、部分集合テストの許可リストへ「群・id をそのまま書いた配列」で追加する（正規表現・件数緩和は不可）。パックからは削除しない。
    2. stage-sketch.js（1442行付近）に「日本語原文の v.note が空なら、他言語パックの説明も表示しない」ガードを入れる。英語側で削除済みのUIを中国語だけ復活させないため。en では見え方が変わらないこと（innerText 基準比較で確認）。
    3. NEEDS_REVIEW.md に「## 2026-09-09 英語側で削除済みの id（ドラフトにだけ残る）」として5 id と削除の経緯（2026-09-04 本人指示）を記録。
  これで R3 の残り（全テスト・かな漏れ検査・日英 innerText 基準比較・ブラウザ表示・スクリーンショット・ロード増分の実測・§4 提案）を最後まで実施し、§7.3 の完了報告の形式で報告する。
  さらに指定外の不整合が出た場合も、同じように止めて報告する（自分で訳語や仕様を決めない）。

【R3e＝再開の指示。前回（R3d）の停止報告への回答】
  推奨案で続行。
    1. かな漏れ検査の式を [\p{Script=Hiragana}\p{Script=Katakana}]（u フラグ）へ変更。中黒「・」は中国語の間隔號として正当なので対象外。発注書 §6 R3 も同じ文言に更新済み。
    2. stage-sketch.js に直書きされた日本語2箇所（会場寸法 18045行付近・書き出し 21975行付近）は、日本語以外の言語で既存の英語へ落ちる狭い修正を許可する（tx / sx / packValue の既存経路を使う。英語が既に無い部分は英語パックへ既存の英訳を移すだけ。新しい中国語訳は作らない）。
       日英の innerText が基準と一致し続けること。修正箇所と前後を報告に書く。
    3. 上記のあと、かな漏れを両言語で再実測して0件を示す（残るなら鍵と理由を表に）。
  そのうえで §7.3 の完了報告（ドラフトの NEEDS_REVIEW 転記を含む）を提出する。判断が要る新しい不整合が出たら同じように止める。
