# アイデア・課題の一覧（2026-08-27・Claude作成／2026-08-28 更新）

本人の依頼「過去に研究されたタスク、いま考えるタスクをリストにして見せて」への回答。
**パスはすべて `shosai-app/` 起点。** 各項目は元文書へ辿れるようにしてある。
リリース判定（β限定・2026-08-27）の後、次の作業を選ぶための材料。

**載せていないもの**: 本人が確定済みの不採用（タイムラインUI、iPadの機構・3Dカメラ、
楽曲のモバイル対応、舞台裏スケッチのUI表示）。再提案はしない。

## 2026-08-28 更新のまとめ（この日に片付いたもの）

- **B-3 解決**（連絡経路＝アプリ内〈感想を送る〉のGoogleフォームで確定・本人承認済み）
- **B-5 完了**、さらに**英語化そのものが完了**。セッションUIに留まらず、UI全体・サンプル
  作品・サインインページ・クイックガイド・**使いかたの冊子（全10章35節）・アプリ内検索**まで
  英語で通る（工程表 `TESTER_MANUAL_PLAN_2026-08-27.md` の該当節）
- **E-4 解決**（夜間ログをコミット／作業前スナップショットを退避。下記E参照）
- **F-1 完了**（3人同時セッションの再検証・**本人が実施し「できている」と報告**。2026-08-28）
- 招待の実務（B-1の名簿投入と招待状の送付）は**本人が直接行う**と確定（2026-08-28）。
  下書きと手順は `docs/INVITE_KIT_2026-08-28.md` にある

---

## A. 下調べ済み・発注書にできる一歩手前

**★A-1〜A-3は 2026-08-27 に完了**（本人が方針決定 → 発注書K/L/M → Codex実装 →
Claude検証 → デプロイ Version `73b1d492` → push）。詳細と実測値は工程表の
「2026-08-27 デプロイ」節。**このAの節は履歴として残す。次の作業はB以降から選ぶ。**

| # | 内容 | 状態と残っている判断 |
|---|---|---|
| A-1 | **矢印を平面図でも描く**＋**描いた図にだけ出す** | **本人決定（8/27）: 平面図で描いたものは平面図だけ、正面図なら正面図だけに表示。** → `docs/WORKORDER_K_ARROW_PER_VIEW_2026-08-27.md`。着手前調査で判明: **逆写像は既にある**（`arrowPointFromScreen`→`fromScreen` が `L.plan` 対応済み。新規作成は禁止）／**既存ショーの矢印は0本**（書き出し8本・ローカル棚618KBを走査）で移行リスクは小 |
| A-2 | **平面図の既定の大きさを実測から決める** | **本人決定（8/27）: PC・スマホ両方。拡大縮小はできるので既定を大きめに。** → `docs/WORKORDER_L_PLAN_DEFAULT_SCALE_2026-08-27.md`。**★8/26の下調べに誤りがあり訂正**——`planBounds()` は駒ではなく**会場寸法で決まる固定値**。真の問題は固定 `pad=104` と袖幅が無関係に決まっていたこと。見込み**約1.15〜1.27倍**（会場形式で変わる。一律1.2倍にはならない）。正面図は対象外（別調査） |
| A-3 | **図の上の付箋（メモ道具）をスマホから外す** | **本人決定（8/27）: 削除。** → `docs/WORKORDER_M_PHONE_NOTE_TOOL_REMOVE_2026-08-27.md`。**入力の道具だけを外し、既存の付箋の表示は残す**（PCで貼った付箋がスマホで読めなくならないように） |
| A-4 | **スマホ縦向きの図の縦幅** | **完了（8/28・発注書N）。** キャンバスを縦向きだけ4:3にし、図は画面上206→275px、下の空きは235→105px。詳細は工程表の「2026-08-28 デプロイ」節 |

## B. β配布の実務（配る前後に必要になる）

| # | 内容 | 状態 |
|---|---|---|
| B-1 | **`GUEST_ACCOUNTS` の投入** | **本人が直接行うと確定（2026-08-28）。** worker.jsは実装・デプロイ済みで、残るはSecret投入だけ。手順・招待状の下書きは `docs/INVITE_KIT_2026-08-28.md`。要点: JSON不正は**fail-closedでサイト全体が503**になるため、投入直後の確認（`/sign-in` が503でない・偽の資格情報が弾かれる・境界401が崩れていない）を必ず通す。旧 `GUEST_USER`/`GUEST_PASS` を残す間は「一人だけ止める」ができない |
| B-2 | **`label` を共有セッションの表示名の既定値に** | 発注書Jで「将来」と明記した宿題。規模: 小 |
| B-3 | ~~**不具合の連絡経路を決める**~~ | ✅ **解決（2026-08-27 本人承認）。** アプリ実装済みの〈感想を送る〉→Googleフォーム（`stage-sketch.js` の `FEEDBACK_URL`）を正式な連絡経路とする。冊子第10章もこの導線で執筆済み |
| B-4 | **実在劇場プリセットの追加** | **第2号 TOHU（モントリオール）を追加（2026-08-28・本人指定）。** 公式技術仕様書 Devis Technique Tohu（2020-02-10版）から実測値を採り、公式サイトの案内値と相互検証（19.4＋3.05＝22.45m、可動席839席が一致）。円形舞台12.8m・グリッド19.4m・可動席11ブロック・最大1004席。**全周形式なので正面図は既存の全周描画に落ち、実在会場の直交ポリゴン前提（realShape）を通らない**——円の輪郭でも壊れない。残り: 配る相手が実際に使う劇場をさらに足す |
| B-6 | **Macアプリを他人にも配れるようにする** | 2026-08-28に受け取りページ（`mac.html`）は作ったが、**アプリはiCloud上の作業フォルダを読む窓**で、そのフォルダが無いMacでは起動しない。人へ配るなら**Web資産を `.app` へ同梱する**改修が要る（`AppConfiguration.swift` のwebRoot解決を、同梱リソース→設定→既定パスの順に）。署名・公証も別途。※`mac.html` と `downloads/` は並行セッションの作業物 |
| B-5 | ~~**セッションUI等の英訳（v2送り分）**~~ | ✅ **完了（2026-08-28）。** セッションUIに加え、UI全体・サンプル作品・サインインページ・クイックガイド・使いかたの冊子・アプリ内検索まで英語化。日本語漏れの検査を回帰テストに常設（`stage-session-i18n` / `stage-samples-i18n` / `stage-manual-help`） |
| B-7 | **中国語・フランス語への多言語化** | **新規（2026-08-28）。** 並行セッションが `i18n-prep/` に準備物一式を作成済み（簡体字・繁体字・仏語のUI辞書ドラフト、劇場用語の対訳表、言語別の文体規則、NEEDS_REVIEW一覧）。**アプリからは読み込まれていない**ので現状への影響はなし。残るは①本体への組み込み②ネイティブ確認。**まだgit未追跡のため、追跡するかの判断が先** |

## C. 調査レポート済み・方式の選定待ち

| # | 内容 | 状態 |
|---|---|---|
| C-1 | **localStorage逼迫（P1-12）** | 実測済み（`overnight-runs/2026-08-23-release-review/P1-12_localstorage-breakdown-report.md`）。60分ショー1本≈280KB・`scenes`が約半分。**4案**: ①null省略（小幅・後方互換対応要）②リビジョン差分保存（効果大・設計変更大）③gzip（効果は実測が要る）④IndexedDB移行（上限問題自体を解消・影響範囲大、音源 `stage-audio-store.js` に先例）。リリース後と本人確定済み |
| C-2 | **音源込みバックアップ** | C-1と同根。リリース後へ先送り確定済み（2026-08-23） |

## D. 大きい構想（研究・文書化済み、未実装）

| # | 内容 | 状態 |
|---|---|---|
| D-1 | **Vision Pro 実寸稽古統合** | 統合方針と実装契約まで文書化済み（`docs/VISION_PRO_REHEARSAL_EXTENSION_HANDOFF.md`・2026-07-30）。舞台スケッチを制作データの正本、Vision Proを実寸で見る側に。**統合コードは未実装** |
| D-2 | **光の意図カードの公開** | ローカル実装済み・未公開（`docs/LIGHT_INTENT_CARD_HANDOFF.md`・2026-08-09時点）。SPECにPhase4=将来検討あり。まず「公開するか」の判断から |
| D-3 | **AI編集の後続段階** | 段階A実装済み（差分を人が承認する方式・2026-08-09）。将来のWeb API/MCP共通契約が `docs/AI_EDITING_HANDOFF.md` にある。現状AI指示はMacアプリ専用（工程6で正常と確定） |
| D-4 | **セットビルダーの外部3Dファイル読み込み** | プリミティブ組み合わせ方式は実装済み。外部ファイルは発注書（`docs/SET_BUILDER_WORKORDER_2026-08-18.md`）で将来枠と明記 |
| D-5 | **髪型・服装カタログの確定** | 11作品の世界観から逆算した候補リストが草案のまま（`docs/AVATAR_LOOK_CATALOG_DRAFT_2026-08-20.md`）。**本人が選んで確定させる材料が揃っている状態** |
| D-6 | **人間旗ポーズ** | FPV発注書（`docs/FPV_WORKORDER_2026-08-16.md`）で将来送りにした小ネタ |

## E. 待ち・小物

| # | 内容 | 状態 |
|---|---|---|
| E-1 | **ポーズ座標3件の差し替え** | 実演由来ポーズが `wide`/`face` を欠いた暫定座標（commit `bc77759`）。**演者モーションモデル側の変換ツール改修待ち（ブロック中）** |
| E-2 | **iPad左レール `⚙` のSVG化** | 他のボタンはSVG化済み、これだけ文字のまま |
| E-3 | **スマホに「端末による違い」の入口** | 幅の要る表なので非搭載。意図的非対応として確定させるか、縦持ち向けの見せ方を作るか |
| E-5 | **発注書の記号「N」が重複している** | 並行セッションが同日に同じ記号を使った: `WORKORDER_N_MANUAL_HELP_2026-08-27.md`（使い方検索）と `WORKORDER_N_PHONE_TALLER_CANVAS_2026-08-27.md`（スマホの図を4:3に）。**発注書側には8/27時点で注記済み**（「発注書N（使い方検索）」と呼び分ける）。2026-08-28に工程表側にも注記を追加し、この一覧へも載せた。他文書の「発注書N」は**ほぼ後者**を指す（8箇所中7箇所）。実装・検証は両方完了済みで実害なし。**改名はしていない**（参照が複数文書にあり、記号は並行セッション共通の採番のため）。両セッションが同じ採番を見に行く仕組みが無いのが根の原因 |
| E-4 | ~~**`overnight-runs/` 未追跡の整理**~~ | ✅ **解決（2026-08-28）。** 実行ログ7本（h/i/j・k/l/m・n・o・p）と棚DB拡張の夜間記録2件を**コミット**（既存 `codex-q.log` と同じ流儀に統一）。公開リポジトリのため投入前に秘密情報を走査＝0件。`2026-08-25-workorder-a/snapshot-before/` だけは復旧用コピーのため `claude-files-archives/shosai-snapshots/` へ**退避**（SHA256照合後に元を削除。`index.html` はgit履歴に無い当時の状態＝退避先の `ORIGIN.md` に明記）。commit `43e4ed2` |

## F. リリース後の検証系（今回「リリース前は不要」と本人確定。課題としては残る）

| # | 内容 |
|---|---|
| F-1 | ~~3人同時の共有セッション再検証~~ ✅ **完了（2026-08-28・本人が実施し「できている」と報告）。** Claude側での再現確認は認証の内側のため不可 |
| F-2 | スマホの保存警告帯の実機確認（意図的に保存を失敗させないと出ない。回帰テスト9件に依存中） |
| F-3 | PCでの楽曲の長時間再生・画面ロック・Bluetooth切替 |

---

## いま残っている候補（2026-08-28時点の見取り図）

配布前に効く順。**B-4 → B-7 → E-3 → C-1** が現実的な並び。

1. **B-4 実在劇場プリセット** — 配る相手の本拠地を足すとβの実用度が上がる。手順は確立済み
2. **B-7 中国語・仏語** — 準備物は揃っている。まず `i18n-prep/` を追跡するかの判断
3. **E-2 / E-3 の小物** — iPad `⚙` のSVG化、スマホへの「端末による違い」の入口
4. **C-1 localStorage逼迫** — テスターが実際に使い込むと現実の問題になる。4案から方式選定
5. **D-5 髪型・服装カタログ** — 選ぶ材料は揃っている。本人が確定させれば実装へ進める
