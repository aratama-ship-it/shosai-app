# 平面図の寸法文字 — 2026-09-10

通常時から「間口 12m」の上部が欠ける不具合を修正。

舞台上端y=24に対し文字中心はy=2（舞台上端-22）で、日本語の文字上端はy=-3.42だった。寸法の文字中心を最小y=12に補正し、日本語では上端y=6.58、英語y=6.78、中国語2種y=5.99になった。数値は1280×720の描画座標。既存の13px書体・配色・舞台の縮尺や位置は維持。

通常表示・全画面・小窓を、4言語とタブレット縦横の6ケースで確認。シーン文書不変、ブラウザエラーなし。before/afterのPNG、各browser-check.jsonに実測値を記録。正本と生成ページ一致・JS構文・git diff --check成功。

Node確認は174件成功、7件未通過。未通過は画面テーマ初期化を受けるテストモックにdatasetがない6件と、会場ライブラリの古い固定版番号1件。今回の文字位置だけを修正前へ戻したメモリ上のソースでも同じ7件が失敗した。比較中に別タスクがキャッシュ版を進めたため、比較ランには固定キャッシュ版の不一致がさらに1件ある。共有ソースの巻き戻し・他機能の修正は行っていない。

ローカルでの確認。公開・Safari・物理端末の確認は含まない。
