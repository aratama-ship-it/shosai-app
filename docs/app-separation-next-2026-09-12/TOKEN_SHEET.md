# アプリ分離・次の実装境界 / 表示仕様

用途: 制作者本人が、制作の書斎と舞台スケッチの配信境界を判断するローカル資料。
公開物ではなく、2026-09-12時点の実装判断と未確認事項を区別する。

- 背景 `#141619`
- 操作面 `#1b1e22`
- 本文 `#eee8df`（既存資料で背景比 14.88:1 を確認済み）
- 補足 `#b4bbc2`（既存資料で背景比 9.35:1 を確認済み）
- リンク `#b5cdd8`（既存資料で背景比 10.95:1 を確認済み、下線あり）
- 境界線 `#87919a`（操作面比 5.21:1）
- 決定済み `#b7d3bf`、推奨 `#ead2a8`、未確認 `#d7b6b2`。色だけで区別せず見出しにも状態を書く。
- 書体 `system-ui, -apple-system, Hiragino Sans, sans-serif`
- 本文 16px / 1.75、補足 14px / 1.7、見出し 24px / 1.45
- 最大幅 1040px、左右 24px（狭幅 20px）、節間 40px
- リンクは最小高 44px、フォーカス輪郭 3px、モーションなし
- 比較はカードの反復ではなく、境界ごとの行と「現在 → 次」の流れで示す。
- 390px幅では比較行を1列にする。意味のある短い日本語は `white-space: nowrap` で語中改行を避ける。

この資料は既存の `docs/release-candidates-2026-09-10/` の配色と間隔を継承する。
