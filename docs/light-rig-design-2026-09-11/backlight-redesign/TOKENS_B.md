# 案B 隔離試作の数値・2026-09-14

本人承認: 案Bの実装・比較試作のGitHub公開と人物修正の公開。距離ドラッグは実寸比較後に最終確定。舞台スケッチのベータ版への統合は含まない。
既存の作業机UI・配色・フォント・数値欄58px・狙いハンドル12pxを維持する（DECISION §11）。
追加操作は照明担当が、編集中のLX cueの空気と客席への狙いを同じ図で調整するため。見本・調整・Undoの既存導線を使う。

- もや: cue.environment.haze、0–100、既定35。札 なし0 / うっすら35 / 濃い70。旧データは読み取り時のみ35とみなし、勝手に書き込まない。
- もや操作: 操作行44px以上、既存文字色 #efe7d6 / 地 #201b16、見出し #9c823f。
- 光条: 世界座標の円錐内を視線方向に18層でサンプル。静止時は画面横192サンプル、ドラッグ・つまみ・再生中は横72サンプル／10層へ落とし、操作終了後に18層へ戻す。指数透過合成、係数0.10/m × (haze/100)^1.4。正面2Dは0.65倍。他図は1倍。校正前の表示近似。
- 立面では人物の奥行きを層境界へ追加し、奥の空気→人物→手前の空気と合成。側面の奥行きはx、正面はy。
- まぶしさ: 光源のみ。視線と円錐角の重み、既存独立倍率0.2–3。通過点は発光させない。
- 対象: 点灯中のair/houseがある場面。floor/backのみは人物修正済み基準版の描画を維持。混在でのみ側面の編集減光を他図と統一する（Q2温存優先）。
- 距離表示比較: 固定帯 / 実寸の切替は表示設定。実寸は舞台と同じpx/m、客席範囲12m、超える点は数値入力。狙いそのものは切替で不変。
- 追加ボタン44px、focus 2px #d3ac59、既存モーションを追加しない。

数値は試作での比較対象。測光値・写真一致・物体間の遮光・リムライトは含めない。

2026-09-14 継続: 交差する8灯の操作中にWebKitで約56msかかったため、操作中だけ72列10層へ調整。静止時192列18層と人物・ハンドルは維持。光条バッファは層ごとに保持し、描画時の再利用競合を避ける。

2026-09-14 導入: 本人直接指示により既存GitHub照明テスター prototype/ へ同じ数値で統合。保存は既存一覧を継続し、初回書込前の控えを保全。本文色/背景のコントラスト13.88:1。
