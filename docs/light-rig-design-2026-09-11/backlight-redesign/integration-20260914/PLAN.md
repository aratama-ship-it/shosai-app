# 既存GitHub照明テスターへの案B統合

本人直接指示「そちらを現在のテスターとして使っているのでそこに導入してくれますか」による導入・公開。対象は docs/light-rig-design-2026-09-11/prototype/。舞台スケッチβ版には配布しない。

公開版5bf35eaを基準に、案Bの描画・cueごとのもや・固定帯/実寸切替・距離ドラッグ・性能改善・保存失敗保護を取り込む。rig-engine、人物描画、見本2ファイルは同一で保持。デザイントークンは ../TOKENS_B.md の値をそのまま採用。距離表示の既定は固定帯、Q2優先の側面適用境界、リム第2段階を維持。

保存は既存キーshosai.lightDesigns.v1を継続し、初回の明示書込の前に既存の生データをbeforeOptionB.v1へ控える。控えに失敗したら書込を中止。読み込みのみでは書かない。案B単独比較ページの専用保存領域は触らず、ファイル経由で読み込める。旧デザインの読込・編集・cue保存・再読込・ファイル往復・失敗保護・前版への復帰を使い捨てデータで検証する。
