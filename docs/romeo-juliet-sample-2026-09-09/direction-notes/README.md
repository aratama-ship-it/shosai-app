# Romeo and Juliet direction notebook

Human entry: index.html. Canonical project notes: scene-notes.json. Scope: the local 12-scene Romeo and Juliet sample in the parent directory. `scene-01-blocking-plan.json` is the detailed, unrehearsed blocking source for Scene 1. Nothing in this folder has been imported into Stage Sketch.

For each new user direction, reread scene-notes.json first, append an entry retaining the original words, and update the corresponding scene summary, directionItems, unresolved and stageSketchHandoff. Keep stable scene/entry IDs. Corrections append an entry with supersedes references; never silently erase previous utterances. Missing values remain null. Keep original story context, user directions, AI suggestions and app-application state distinct. A count of all performers is not a count of story characters. TYBALT aliases are resolved in the handoff; the script spelling is not changed by these notes.

`RJ-CONVENTION-02` fixes the production at 10 performers. The current 10 named roles use one performer each; performers rotate into ensemble functions when their named role is not required in a scene. In Scene 1, only Benvolio and Tybalt appear as named characters; the other eight role-holders temporarily perform anonymous street-crowd functions. Their four-corner pairings, outcomes, reference positions, and approximate timing are fixed in `scene-01-blocking-plan.json`. Performer names, body conditions, actual-venue transformation, choreography, and technical levels remain unresolved.

Scene 1 now has a generated and validated Stage Sketch v3 draft at `../stage-sketch/scene-01-stage-sketch-v3.json`. It remains unimported.

Scene 1 overrides the global left/right house convention. Benvolio and Tybalt remain at center while the other eight performers form four pairs and brawl in the four corners. Benvolio tries to stop the brawl; Tybalt refuses that gesture and turns it into a challenge. The current version is an approximately 3-minute draft: 115 seconds of simultaneous brawls and stopping gesture, 40 seconds of three resolutions, 25 seconds of dispersal toward the challenge, then a zero-duration challenge/blackout marker before contact. Pair assignments, outcomes, reference dimensions and normalized positions are recorded; actor conditions, actual-venue conversion, safe choreography, lighting levels and rehearsal timing remain unresolved.

Scene 11 uses `humanBoundaryScore` for the user-directed human-river expression of the non-interference region. Its movement verbs and prohibitions are the stable generation rules; performer count, dimensions, current direction, contact level, and release action remain unresolved. The six entries in `referenceOptions` remain unselected supporting or alternative systems. Preserve source links, checked dates, and adoption states when generating later Stage Sketch material.

Scene 12 begins with grief for Romeo and Juliet. Friar Lawrence stays at stage center while the surviving people join the gathering around him one at a time before the final reconciliation excerpt. Preserve this sequence when preparing later Stage Sketch material. The exact cast, character order, gathering paths, amount of explanatory dialogue, and transition from Scene 11 remain unresolved.

Rebuild with: python3 build_notes.py (from this directory). It writes only this folder's index.html. Review the page and relevant text after each update.

The builder also reads ../scene-samples/draft.json and joins scenes by scriptSampleId to display all 12 short script excerpts. It uses saved userRevisionJa when non-null, otherwise jaDraft; browser-only unsaved edits are not read. Omission notices are displayed outside the script. Do not copy or edit script text in scene-notes.json. Rebuild this notebook after updating the canonical short samples. Noted scenes show the script beside the direction notes on desktop and above them on mobile; pending scenes show the script when their heading is opened.

For later app generation, recheck ../../../stage-sketch.js and the active import/export schema. The scene handoff remains a planning brief, while `../stage-sketch/scene-01-stage-sketch-v3.json` is the separate validated import candidate. The roster is fixed at 10 and Scene 1 has an approximate 3-minute target. Normalized reference positions exist; exact actor dimensions, actual-venue positions, rehearsal timing and fixture levels are unresolved. Do not write browser storage without the user's instruction.

On this Mac, the existing local server serves the parent sample folder at http://127.0.0.1:18743/ . The notebook URL is /direction-notes/ . If needed elsewhere, run python3 -m http.server 18743 --bind 127.0.0.1 --directory .. from this folder. The server is only a preview, not publication.
