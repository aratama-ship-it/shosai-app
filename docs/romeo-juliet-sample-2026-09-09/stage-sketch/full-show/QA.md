# 全体サンプル検証 — 2026-09-11

## 確認済み

- `validate_sample.mjs`: 現行MCPの参照検証とサンプル内容の検査に合格。3セクション・28配置・10名・13セット・仮尺1745秒。19キューの合計を維持。
- 台詞47ブロック（追加提案9）、動作74ブロックを元の通し稿と照合。全文と由来をhandoff JSONに保持。ネイティブ説明は200文字以内。
- `check_sample.py`: 127.0.0.1:18759で現在の `stage.html` を配信し、新規Chromiumコンテキストで実ファイル選択→比較→「別のショーとして開く」を実行。
- 全28配置をシーン一覧から順番に開いて表示。ID・座標・向き・姿勢・色・動線・照明意図・尺・説明・仮面の装着参照を、アプリから再取得した値と照合。
- 場面説明の短い編集と復元、ファイル名入力フォームを経たJSON再書き出し、再読み込みに成功。
- オフラインHTMLを別の一時フォルダへ単独コピーし、通信オフの390px／1440pxで閲覧。28配置の全文メモが存在し、ダウンロードした2つのJSONが元ファイルとバイト一致。ページエラーなし、横はみ出しなし。
- 保存用HTMLのdesign-lintはNG 0／WARN 0／測定不可 0。コントラスト最小5.94:1。冒頭・舞踏会・結婚・人の川・終幕の実アプリ画像、および保存ページを目視確認。
- 書き出しと確認に使ったアプリコードのSHA-256はhandoff JSON内の記録と一致。アプリ本体・MCP本体はこの作業では変更していない。

## 検証で修正した互換性

- 原図とアプリで前後の角度定義が逆なので変換。背中の7名は180°へ。
- `lie_back` は現行ブラウザで立ち姿へ戻るため、ブラウザが保持する `supine` に変換。
- 直線ルートの省略制御点にアプリが0.5/0.5を補うため、書き出しに中点を明記。
- 壁の奥行はアプリ固定値0.3mに合わせた表示用仮値。
- 説明欄へ長文を入れると編集時200文字で切れるため、短い説明と全文の別添を分離。

## 範囲

- 本人が利用中のブラウザ、既存ショー、共有セッションへは適用していない。上記は隔離環境での読み込み確認。
- ブラウザのファイル出力は既存のダウンロード方式で確認。OSネイティブの保存先ダイアログ、Mac専用アプリ、実機、別システムへの組み込みは未確認。
- 寸法・各配置の尺は未稽古の仮案。人体や会場の実測ではない。音源、照明機材、小道具の全駒、台本キュー専用機能は今回の出力対象外。
- 参照バリデータが返す6件の動線警告は演出検討用の注意。実演上の接触・動線の安全をこの検証で判断したものではない。

証拠: `qa/data-checks.json`, `qa/browser-checks.json`, `qa/native-roundtrip.json`, `qa/native-*.png`, `qa/download-page-*.png`, `qa/design-lint/report.md`。
