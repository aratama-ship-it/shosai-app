# 数値基準 — UI改善準備資料と製品の初版案

2026-09-10 / design-web手順による。色・書体は既存style.cssの紙/机の系統を参照。
この資料の値をそのまま製品全体へ上書きしない。製品の既存スキンは維持する。

## 判断資料 index.html（確定値）

| 用途 | 値 |
|---|---|
| 背景 / 本文 | #efe7d6 / #2b2620 |
| 補助文字 / リンク・フォーカス | #6a604e / #8f3e1e |
| 白い面 / 区切り線 | #faf6ee / #6a604e |
| 書体 | Hiragino Sans, Yu Gothic, sans-serif |
| 大見出し / 中見出し / 本文 / 注記 | 32px / 24px / 16px / 14px |
| 行高 | 本文1.7、見出し1.4 |
| 本文の行長 | 最大50em（日本語で約50字） |
| 余白スケール | 4,8,12,16,24,32,48px |
| 最大幅 / 外周 | 1120px / 16px（左右） |
| ブレークポイント | 700px。小画面は1列 |
| 操作領域 | 最小44×44px |
| 枠 / 角丸 / 影 | 1px / 0 / なし |
| フォーカス | 2px、外側2px |
| 動き | なし。Reduced Motionでスクロールも即時 |

コントラストは共有contrast.mjsで実測:
- #2b2620 on #efe7d6: 12.19:1
- #6a604e on #efe7d6: 5.03:1
- #8f3e1e on #efe7d6: 5.94:1
- #2b2620 on #faf6ee: 13.91:1
- #6a604e on #faf6ee: 5.74:1
- #8f3e1e on #faf6ee: 6.77:1
文字/リンクはいずれも本文基準4.5以上。ブラウザ上の実描画・design-lintは未実施。

## 製品側（本人指定と既存値・実装案）

| 項目 | 数値・状態 | 根拠 |
|---|---|---|
| 会場モーダル・広幅 | 約80vw | 本人指定。980px上限からの変更 |
| 会場モーダル・狭幅 | calc(100vw - 32px)、高さ上限calc(100dvh - 32px) | 実装案 |
| カスタムステージの編集履歴とグリッド倍率 | 戻す／進める／−／＋を右下へ重ねる、各44×44px、4列、gap4px、外周4px | 編集履歴と表示倍率を同じ操作群へ並べる。戻す／進めるは舞台制作内容だけを対象にし、表示倍率は履歴へ含めない。既存の机色・線色を継承 |
| カスタムステージの追加舞台 | メイン形状の直後に四角／丸／重なりを合成の均等3列、各44px以上、矩形最小辺0.4m、円最小直径0.4m、円周24分割 | 客席・舞台袖と同じドラッグ操作。既存の舞台面へ接触または重なる追加面だけ確定し、同じ床・可動範囲・全体寸法として扱う |
| 追加舞台の移動／合成 | 追加形状は直接ドラッグ、移動開始0.14m、合成ボタン高44px以上、操作間gap8px、外周からの可動余白0.5m | 接続が切れる移動は確定しない。重なりがあるときだけ合成を有効にし、合成後は内部境界を外周から除外して可動範囲を再計算する。Undoで合成前へ戻せる |
| カスタムステージの客席／舞台袖配置 | 5番・6番とも四角／丸／重なりを合成の均等3列、各44px以上、矩形最小辺0.4m、円最小直径0.4m、円周24分割、操作間gap8px。客席はrgba(168,75,38,0.25)＋実線2px、舞台袖はrgba(240,231,214,0.12)＋破線8px/5px | 両方を同じドラッグ操作へ統一。合成時は重なる形の内部境界を隠し、Undoで解除できる。選択中は真鍮色3pxで示す。「全周に配置」は360度ステージのときだけ5番内に表示する |
| 会場内部 | 320px設定列＋主図、gap16px、狭幅では1列 | 実装案。現行DOMを見て調整 |
| 追加/設定モーダル | 上限960px、狭幅は左右16px、内容は縦スクロール | 実装案 |
| 新しく触るボタン | ヒット領域44×44px以上、アイコン18〜20px | 初版基準 |
| 右上ボタン | 44px | 現コードの共通値 |
| 通知ベル | 現コード32px、SVG18px、点7px、周期1800ms | 現状記録。タッチ領域44pxへの調整を受入確認で検討 |
| 追加モーション | 通常UI開閉0〜160ms、通知以外の装飾モーションなし | 実装案 |
| 説明ホバー | 表示300ms / 離脱200ms、ポップオーバー内へ移動中は維持 | 実装案。タップ/キーは即時 |
| 操作群 | gap8px、項目間16px、節間24px | 実装案。既存シーン操作の3pxは幅を実測して判断 |
| JSON書き出し前の名前確認 | 既存モーダル幅380px、左右20px余白、入力80文字、操作ボタン1:1・44px以上・文字14px、起動キー⌘S／Ctrl+S | 既存のモーダル・入力欄・操作列を再利用。新しい色・動きなし。文字入力中も書き出しを起動する |
| 会場ライブラリ保存時の名前確認 | 既存モーダル幅380px、入力最大100文字・必須、操作ボタン2等分・高さ44px、var(--sans)・14px・var(--milk)・字間0.15em | 常設の会場名は任意入力。ライブラリ保存時だけ確認モーダルで必須にし、常設欄の値があれば初期表示する。やめる／保存は枠色以外の寸法と文字を揃え、既存の色・動きを継承 |
| ステージ・客席・舞台袖の重なり警告 | 既存モーダル幅420px、本文11px・行高1.7、優先ボタン2等分・高さ44px、gap12px、背景var(--desk-2)、枠var(--line-dark)、主要枠var(--rust) | 異なる領域が重なったドラッグ確定時に即時表示。優先する2領域を明記し、選ばれなかった側は重なった部分だけを切り取る。閉じるだけの操作は置かず、選択結果を1回のUndo単位にする |
| ショー要約と詳細設定 | 要約タイトル13px、版10px、要約内gap8px、詳細設定44px以上、モーダル上限620px・左右20px余白 | 常設パネルは識別情報と入口だけ。編集・一覧・入出力・劇場形式は既存部品のままモーダルへ集約。新しい色・動きなし |
| ショー／劇場パネルの分離 | 左列でショーの直後に劇場、要約タイトル13px、補助10px、入口44px以上、パネル内gap8px。会場ライブラリ操作は編集パネル右下でPCは均等3列・gap6px・高さ44px以上、文字はvar(--milk)・14px・var(--sans)、640px以下は1列 | ショーは作品情報、劇場は会場情報として分離。劇場の主操作名は「劇場セットアップ」とし、右側の編集パネル最下部へ「ライブラリに保存／会場を書き出す／会場を読み込む」を同じ操作群として並べる。プリセットまたは規模を変えた時は床形状・ステージ形式・客席・天井を同じ編集状態へ即時反映する。既存の机色・線色・書体を継承 |
| 劇場形式プリセット／カスタム入口の文字 | var(--sans)、12px、var(--milk)、標準ウェイト・標準字間 | 同列の2入口を既存の劇場形式プリセット表示に統一。背景・枠・寸法は変更しない |
| 中央バーのUndo／Redo | PCは各40×27px・SVG 16px・ボタン間0px・次群まで10px、iPadは既存44×44px | 中央バーの最左に置き、既存の線色・無効時opacity 0.28・フォーカス表現を継承。新しい色・動きなし |
| 中央バーの表示プルダウン | PC幅88px・高さ27px・文字10.5px、4択、右寄せ | 正面／平面／両方①／両方②。両方①は正面が上、両方②は平面が上。既存の色・フォーカス表現を継承し、新しい動きなし |
| 中央バーのアニメ操作 | 入口27px、時間欄190px、0.2〜3秒・step 0.05 | 照明名の右。ホバーまたはフォーカスで下へ展開し、タップ時はチェックボックスへフォーカスして展開。新しい装飾モーションなし |
| 平面図の動線生成 | 既存の図上ボタン高26px、隣接gap8px | 「動線を描く」の直後に「次のシーンから動線を引く」。対象がない時は無効表示 |
| シーン行の補助操作 | 常設4アイコンを撤去、選択行に28pxの「…」入口、展開内は34px以上 | 入れ子・名称変更・範囲まとめを一つの補助メニューへ退避。ダブルクリック名称変更とドラッグ順変更は維持 |
| 背景設定モーダル | 上限760px・左右20px、2列・gap16px、狭幅760px以下は1列、主要操作44px以上 | 地色・描画・スクリーン文字・写真を既存パネルから集約。塗る／消すは文字ボタンで開始し、既存の保存値を利用 |
| パネルの初期幅 | 既存の保存値・クランプを継承 | 固定の新値を未確認で押し付けない |
| アニメ時間 | 0.2〜3秒、step0.05、未設定時オン | 既存値と本人指定 |
| 天井高 | step0.1m。min/maxは既存正規化で確認 | 初版案。4.5mを受入例にする |
| 連続選択肢の選択枠 | 1px、var(--rust)、選択項目を隣接項目より手前に表示 | 選択中は必ず四辺を閉じる。隣接境界を省略する場合も、選択中の左辺を1pxで復元する |
| 矛盾の幾何誤差 | epsilon 0.01m | 仮案。現行FOLLOW_M=0.6/WARN_M=2.5は別用途 |
| キャラクター | 3体、静止、各外枠32〜40pxを出発点 | 体数は本人指定、サイズは仮。透過余白を目視確認 |

単位やスライダー値は既存モデルの意味を継承し、表示を小さくするために入力精度を落とさない。
製品で新配色を使う時は全スキンと状態で測る。資料のコントラスト合格を製品の合格に流用しない。
