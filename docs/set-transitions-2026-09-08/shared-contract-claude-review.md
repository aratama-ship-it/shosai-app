レビュー完了です。2ファイルのみ読み取りで確認しました（stage-sketch.js等の既存ソース行番号の裏取りは範囲外のため未実施。§1の記述は前提として扱っています）。

## 判定

**進行可。** 契約v1とfixtureに、次の人物別試作を止める重大な矛盾はありません。数値例3件はすべて契約の計算規則と一致することを手計算で検証しました。軽微な参照不整合1件と、反例の不足があります。

## 検証した数値（④）

- **通常例**: 出番[-4,0)→歩く[0,2)→carry-start=latest(q12=0, walk-A-end=2)=2→運ぶ[2,5)→置く[5,7)→hold-A[7,8)→pickup-start=latest(put-end=7, walk-C-end=8)=8→持直し[8,10)→exit-start=latest(pickup-end=10, q13=10)=10（cue-wait[10,10)=「待機なし」、§4.2の待機0許容と整合）→搬出[10,15)。締切15000で不足max(0,0)=0、余裕0。fixtureの`expected.normal`と一致。
- **出番4秒延長**: 歩く[4,6)→運ぶ[6,9)→置く[9,11)→hold-A[11,11)→持直し[11,13)→exit-start=latest(13, q13=10)=13→搬出[13,18)、不足3000ms。MD§7合否条件2および`expected.appearance-end-at-4000`と一致。合図Q13が早い場合もlatestが吸収し、§4.3「早い合図は予約にならない」と矛盾しません。
- **Q13未知**: latestが未知を0扱いしない規則（§4.2）により exit-start以降が未確定、pickup-end=10000などQ12側は残る。`expected.q13-offset-null`と合否条件5に一致。

各人物の区間重複なし（[開始,終了)規則で境界接触は非重複）。hold-Aの終了がpickup-start（独立条件のlatest）を参照する構造は§4.3の循環禁止規則を満たしています。

## 具体的な指摘

1. **参照切れ（③に該当・唯一の実不整合）**: fixtureの`points`のうち`q12`/`q13`の`ownerRef`が`"cue-plan-q12"`/`"cue-plan-q13"`ですが、この IDはfixture内のどこにも存在しません。他のpointのownerRefはすべてactivity/windowに解決されます。契約§4.2「IDと所有元を持つ」・§6/合否条件8「参照切れは適合へ変換しない」に照らすと、契約適合性を示すべきfixture自身が参照切れを含む状態です。
2. **交代・待機の非対称（①）**: 出番延長ケースでは C が walk-C-end=8秒から pickup-start=11秒まで手空きになりますが、対応するactivityがありません。契約上は「担当を保持しない者の空き」は予定不要なので矛盾ではありませんが、hold-A（Aが待つ側）だけが明示されている非対称は、試作側が「延長時にCの待機を生成すべきか、空きのままか」を誤読する余地があります。契約§4.3に一文（保持しない到着待ちは予定を要求しない旨）を足すのが最小です。
3. **二重正本の観点（②）**: 予定・時点・所要はfixture内で単一正本になっており問題なし。`expected`配列は計算済み時刻を含みますが、テスト期待値として明示ラベルされており、§4.2「計算済み時刻を別保存しない」への違反ではないと判断します。念のため`scope`文に「expectedは検証期待値であり保存対象ではない」と一言足すと誤認を防げます。
4. **改訂・参照切れ規則（③）**: §6の失効マトリクスとrevision組の記録方式に矛盾なし。fixtureは全activityがrevision:1のみで、改訂検証用データはありません（下記の反例参照）。

## 最小修正

- fixture `points[q12].ownerRef`→`"origin-q12"`、`points[q13].ownerRef`→`"origin-q13"`（または実在するcue ID）に変更。
- MD§4.3へ「担当を保持しない到着待ちの空き時間は予定を要求しない（保持する場合のみactivity化）」を1行追記。

## 最小限追加すべき反例（⑤）

fixtureの`expected`または追加ケースとして:

1. **締切のみ未知**: 合否条件6前半（不足秒数を出さない）に対応するケースが未収載。deadline未知＋所要既知の1件。
2. **未対応assignment**: 全assignmentがconfirmedのため、合否条件4（personId:null/unresolvedを適合にしない）を検証するレコードが1件もない。
3. **反復cue入力**: 合否条件7の「非対応として保持・表示」を検証する、同一cueIdを2時点に置いた不正入力1件。
4. **既知枠12秒/所要18秒**: 合否条件6後半の数値例がfixtureに未収載（MD記述のみ）。

## 残る要確認

- §1のstage-sketch.js行番号・project-store.jsのversion 3経路は今回の範囲外で未裏取り。製品接続前の共同監査（§6）で確認される前提として扱いました。
- 出番延長ケースでCが8〜11秒空きのままでよいか（平台やほかの支え作業がないか）は架空例の仮定であり、実演目に適用する際は本人確認が必要です。
- 反復cueの発生単位・製品保存位置は§8のとおり未確定で正しく、v1試作の妨げにはなりません。
