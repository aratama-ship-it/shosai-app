# 全画面の入れ替え操作 — 2026-09-10

本人指定: 右上のアイコン、右下の小窓、未使用ショートカットで正面・平面を入れ替える。

- 右上の終了アイコンの左に44pxの入れ替えボタンを追加。SVGは20px、間隔8px。
- 右下の小窓クリックに加え、全画面中のXキーで同じ交換関数を呼ぶ。小窓には「⇄ X」を表示。
- ショートカット一覧、日英の使い方冊子、両ボタンのaria-keyshortcutsを更新。アイコンの名前は既存の4言語訳を利用。
- 通常画面、文字入力、修飾キー、IME、長押し、ダイアログ、3Dカメラ中はXで図を変えない。

検証: 関連Nodeテスト161件成功、生成ページ一致、git diff --check成功。独立したChrome環境で8ケース（日英・簡繁、タブレット縦横、全画面APIなし/拒否）を確認。アイコン/小窓/X/Enter/Spaceでの交換、位置・寸法、シーン更新、データ不変、終了後の配置復元を確認。日本語PCケースで各入力ガードと実際の3Dカメラを確認。

`browser-check.json` と `unit-tests.log` に結果、PNGに表示を記録。`browser-check.cjs` はこのMacのPlaywright環境・ポート8847で再実行可能。

ローカルのみ。物理iPad・Safari・公開環境は今回の確認に含まない。
