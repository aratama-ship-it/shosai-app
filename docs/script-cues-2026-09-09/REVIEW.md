# 台本・キュー設計の相談・採否と検証

2026-09-09 / Astra / 製品未適用

## 相談の実行状態

- Astra: 現行の場面正規化・Undo・v4書出し・MCP v3検証・明かり差分印刷を確認し、3案比較と設計計画を作成。
- Claude Code Fable: 接続確認成功。添付前の相談文は claude-request.txt。最新の匿名相談文は claude-pdf-request.txt。安全モード・Read/Glob/Grep限定・新規・会話非保存を維持し、本人指定のfableだけを一時ラッパーに指定。
- Fableレビュー本体: 未実行。自動承認レビューが「内部設計資料/ソースをClaude外部サービスへ送る具体的ペイロードと宛先の明示承認不足」として拒否。本人へ具体的範囲を示して確認中。成功したレビューや両モデルの合意と表現しない。
- 転換側: 既存タスク「舞台転換機能の設計を詰める」へ接続案を相談し、Astraの回答を受領。この連絡のための追加外部モデル呼出しは依頼していない。

## 転換側との合意・修正

1. 合図定義のproject共通正本、台本任意、逆引きは派生、台本操作による自動GOなしに双方賛成。
2. cueの複製を一律に新IDへ変更する案は採らない。同じ合図を共有する転換の複製はcueId維持、独立した合図にする場合のみ新ID。
3. 合図の内容/位置変更は担当競合と所要の条件を変え得る。幾何の確認は保持し、影響する担当/所要だけ再確認する。
4. 担当交代があるという本人回答を転換側から受領。HANDOFF_NEXT_2026-09-09.mdを読み、区間担当・前提工程による準備・合図先着時の扱いを台本側の計画へ反映。
5. 主GO位置0〜1は初版の提案。最終schema・発生単位の契約・実例は未確定。

転換側の担当交代案には同タスクでのFableレビュー記録があるが、それは今回の台本UI/アンカー設計をFableがレビューした証拠ではない。

## 保留

実台本PDFの改訂前後、主な使用端末、Fableの独立ブレスト/レビュー。本体の実装・保存試験・実端末・公開は未実施。要望の会話画像4枚は受領済み。
固定例は行単位の後ろへのキュー追加/付替え・改稿による要確認・架空新版で失った位置の修復・Undo/Redoを試すもの。本文中の単語範囲選択、PDF/Word取込、永続保存、担当交代の再生は未実装。

## 検証

配色6組: design-web/tools/contrast.mjsでAA本文4.5以上。
設計固定例: Chromiumで18項目成功（browser-checks.json）。付替え/Undo/Redo、旧新台詞の表示、明示確認、改訂による参照欠落/修復/取消、繰返し追加、キーボード選択後のフォーカス、4画面幅の横溢れ、JSエラーなしを確認。
表示検査: design-lintで390×844と1440×900、NG 0 / WARN 0 / 測定不可 0。初回の参照リンク44px未満を修正し再測定。最小コントラスト5.03:1。report.mdに詳細。
目視: preview-desktop.png、preview-mobile.png、preview-revision.pngで台本/キュー/改訂後の位置不明表示を確認。スマホの製品詳細面切替、実タブレット、印刷の実改頁、日英切替は未検証（この設計資料は日本語のみ）。
範囲: 固定例の成功を製品の保存・取込・担当交代・本番制御の検証と解釈しない。
既存ファイル: baseline.jsonで10製品ファイルのハッシュ一致を確認。転換側のSPEC/OPERATIONSの変化は別タスクから通知された担当交代の設計更新であり、このタスクから書き換えていない。

このMacのプレビュー: http://127.0.0.1:8937/script-cues-2026-09-09/index.html
停止後の再起動: shosai-appをcwdに `python3 -m http.server 8937 --bind 127.0.0.1 --directory docs`。
静的ファイルの最新入口は pdf-layers.html。外部公開なし。Fableの実レビューが返ったら本書の採否とPDF_LAYER_PLAN/pdf-layersを更新する。

## 添付受領後の設計更新

4画像を読み、PDF台本のページ/行/単語へ配置・動線・照明/音響キューを付け、改訂PDFでも注釈を保って再調整したい要望を確認。PDF_LAYER_PLAN.mdを最新計画、pdf-layers.htmlを主な確認先とした。元画像/氏名/連絡先をHTMLへ転載していない。添付内の紹介や返信のやり取りを外部連絡の指示として実行していない。
他製品の消失については利用者の経験として記録。Stage Write公式Digital Scriptにはページ置換、App Storeの変更履歴にはレイヤーの記載があるため、現行製品にその機能がない/必ず消えるとは断定しない。
転換側からは基本整合と回答。pathRefの安定参照を未確定のまま仮定しないこと、合図の意味が変わったか不明な場合も担当/所要を再確認することを追加した。
Fableの新しい相談文はclaude-pdf-request.txt。匿名の機能要件と設計案だけ、追加ファイル読込みなし。原画像/私信全文/氏名/連絡先/ソースコードを送らない範囲として、本人へ送信確認中。前回拒否後の外部実行はまだ行っていない。
前段の18項目は文章編集の固定例についての証拠。PDF差替えを模した新しい固定HTMLについての確認結果は別に記録する。


## PDF差替え固定見本の検証（添付受領後）

- 対象: pdf-layers.html。実PDFではなく固定レイアウトのHTMLによる操作シミュレーション。
- Chromiumの操作確認28項目が成功（pdf-browser-checks.json）。注釈3件のID/接続保持、選択した注釈だけの移動、位置未対応の確認防止、明示確認、差替え採用/取消/Undo、改頁、台詞削除後の付替え、非表示中の検査、未確認を残した採用を確認。
- キーボードの選択/付替え/フォーカスを確認。同じ語に3件を付けても、アンカーは保ったままラベルを分けて紙面内に表示。
- 1440/1024/768/390pxの4幅でページ全体の横溢れなし。狭幅の台本内部パンは意図した動作。JavaScriptエラー0、node --check成功、資料内の相対ファイルリンク欠落0。
- design-lint最終測定: 390×844、1440×900、NG 0 / WARN 0 / 測定不可 0（pdf-design-lint/report.md）。初回は単語ボタン2種の幅が26pxだったため44pxに修正。最小コントラスト5.03:1。
- 目視: pdf-preview-desktop.png / pdf-preview-mobile.png / pdf-preview-shift.png / pdf-preview-repair.png。無効操作の区別、日本語見出しの語句単位の改行、台詞削除からの修復時にラベルが重ならないことを確認。
- 製品10ファイルはbaseline.jsonのSHA-256と一致。製品コード・生成物・元画像・実台本を変更していない。公開なし。
- 未検証: 実PDFの取込/文字選択/座標変換、永続保存、印刷PDF合成、実タブレット、実際の舞台転換データとの接続。自動検査の成功をこれらの証拠としない。
- Fable: 依然として今回の外部相談は未実行。相談文の送信承認待ち。

最新プレビュー: http://127.0.0.1:8937/script-cues-2026-09-09/pdf-layers.html


## 本人のドラッグ＆ドロップ希望を反映（2026-09-09）

本人から「UIは基本的に感覚的なドラッグ＆ドロップなどを使いたい」と指定。PDF_LAYER_PLAN.md §2と人間向けpdf-layers.htmlを更新し、ドラッグを主導線、クリック/タップ/キーボードを代替操作に変更。既存の注釈3件をつかんで単語へ付け直す、一括選択して同じ量だけ動かす操作を見本に追加した。製品の新規キュー登録・実PDF・ページをまたぐドラッグ・ラベル位置だけの独立ドラッグは未実装。

- pdf-drag.jsに局所見本のPointer Events処理。ドラッグ中は元データを保持し、候補語・移動対象・件数を表示。正常なドロップで1操作として確定、意味の確認は残す。
- 候補外/Escape/pointercancel/捕捉喪失/画面離脱/表示操作への切替は取消。範囲外の対象を含む一括移動は全体を取消。1回Undoで全対象を復元。比較用の旧版は変更しない。
- pdf-drag-checks.json: Chromium18項目成功。マウス、Chromiumの模擬タッチ、途中の状態不変、IDとcue参照の保持、取消・一括Undo・旧版比較の編集防止を確認。実機の操作感の評価ではない。
- 既存操作28項目も再実行し成功（pdf-browser-checks.json）。本体保存試験と読み替えない。
- pdf-drag-design-lint/report.md: 390×844 / 1440×900、NG 0 / WARN 0 / 測定不可 0。追加色なし。pdf-drag-preview.pngで候補語、持っている注釈、移動先の表示を目視確認。
- inline JS / pdf-drag.jsともnode --check成功。既存製品10ファイルはbaseline.jsonとSHA-256一致。
- ドラッグ希望はFable用の匿名相談文へ追加。外部相談は依然未実行で、送信承認待ち。転換側へ主導線の変更と参照保持/自動GOなしを共有した。
- 次の局所検討: 新規の配置図/キューを一覧から台詞へ置く入口、実PDFと実端末でのページ送り・ズームとドラッグの両立。

最新版の確認先: http://127.0.0.1:8937/script-cues-2026-09-09/pdf-layers.html#demo


## 台本あり／なしと左右配置の比較（2026-09-09）

本人の「右に台本情報、左に全体メニュー、iPadの押出式も検討、台本あり／なしを分ける」という希望をWORKSPACE_LAYOUT_PLAN.mdへ反映。workspace-layout.htmlで左常設/押出式と台本あり/なしを比較できる。既存iPad PWAのstage-sketch.js:11554以降とstyle.css:10433以降を確認し、同一パネルを移動して保存を一系統に保つ方針を継承した。製品側には適用していない。

- 操作確認25項目が成功（workspace-checks.json）。左右開閉、幅の再配分、非重複、台本モード切替、頁/スクロール/注釈選択保持、明示的シーン表示、狭幅の一面切替、フォーカスを確認。
- 初回はスマホ幅で台本を閉じると以前の左メニューが再表示されたため、舞台へ戻るように修正して再確認した。
- 1440/1280/1024/820/390/844px（横向きの高さ390px含む）の幅でページ全体の横溢れなし。
- 押出・左常設・台本なしの3状態それぞれ390×844と1440×900でdesign-lint。workspace-lint-push/dock/withoutの3報告ともNG 0 / WARN 0 / 測定不可 0。
- workspace-push-desktop.png / workspace-dock-desktop.png / workspace-tablet.png / workspace-mobile.png / workspace-without.pngを目視。構成比較のための固定HTML/SVGで、実PDFの組版/拡大率や実3D・実端末を検証したものではない。
- 現在の表記を「シーン4」へ統一。配置という別の新概念を増やさず、同じsceneRefを保った。
- 構文検査成功、参照ファイル欠落0、製品10ファイルのSHA-256はbaselineと一致。
- 次の推奨作業は、選んだ構成で左のシーン1件/キュー1件を右の台詞へ新規に置き、中央表示→モード切替→保持を一巡。実PDFの旧新版各1本で試す。新構成でのドラッグはまだ未実装。
- 転換側は別途、本人が承認した人物・合図・時間の共通契約草案の単独書き手。こちらから独立した予定ID/PDFアンカー分離、同一起点の相対時間と起点間未知、改訂失効範囲を限定回答した。製品保存や人物schemaが確定したという意味ではない。
- Fableの相談文に左右配置/モード希望も追加。外部レビューは依然として送信承認待ちで未実行。

このMacの最新構成確認先: http://127.0.0.1:8937/script-cues-2026-09-09/workspace-layout.html


## 仮PDFで割当を一巡（2026-09-09）

本人は画面構成に賛成し、制作中の実台本の代わりに適当な素材で進めるよう指定。Astraで架空の旧版/改訂版各2頁を作成し、assignment-trial.htmlを追加。既存の比較見本を残して入口リンクを追加した。

- PDF: output/pdf/fixture-script-original.pdf と fixture-script-revised.pdf。ReportLabで生成、埋込み日本語フォント、Poppler144dpiで全4頁を画像化して目視確認。pdfplumberから全16本文行の文字列と矩形を抽出し、元の仮原稿と一致を確認。ブラウザはこの生成済み画像/矩形を使用する。汎用PDF読込み/OCRや任意単語選択の完成ではない。
- 操作: assignment-checks.jsonの28項目とassignment-cancel-checks.jsonの6項目が成功。新規割当、同一IDの付替え、Undo、版変更による要付替え保持、頁をまたぐ手動付替え、モード往復、明示舞台表示、候補外/Escape/幅変更/pointercancelの取消、同じ行の2注釈、キーボード、Chromium模擬タッチを確認。模擬タッチは実iPadの証拠ではない。
- UI: assignment-lint/report.mdで390×844/1440×900、NG 0 / WARN 0 / 測定不可 0。PDF画像内の文字はこのDOM測定に含まれず、全頁の画像で別確認した。assignment-desktop/mobile/revision.pngでラベルと本文、左右配置、要付替えを目視。スマホでは紙面の最低幅360pxを保つため内部の横スクロールがある。
- 5幅（1440/1024/820/390/844px、高さ390pxも含む）でページ全体の横溢れなし。JavaScriptエラー0、node --check成功。baselineの製品10ファイルはSHA-256一致。変更はこの資料フォルダ内のみ、外部公開なし。
- 制限: 再読込みで初期化。製品保存、共有、転換/人物への実接続、一般PDF、実端末、印刷合成は未実装/未確認。meaningReviewはunconfirmedを保持し、紙面への配置をGOの意味/時間条件の確認済みとしない。
- 舞台転換側の共通契約SHARED_PEOPLE_CUES_TIME_CONTRACT_2026-09-09.md §3〜6を読み、台本側3条件と矛盾がないと回答。共通契約は相手側の単独管理で、こちらに予定や時間の新しい正本は作っていない。
- Fableの外部レビューは引き続き未実行。今回の操作試作はAstraによるもので、独立レビュー済みと扱わない。

最新の操作確認: http://127.0.0.1:8937/script-cues-2026-09-09/assignment-trial.html


## 未割当警告と道具の隣接配置（2026-09-09）

本人から「未割当の警告」「割当道具を平面図と台本キューの間へ」の指定。assignment-trial.html/assignment-trial.jsを更新。変更前の2ファイルはbefore-warning-and-dockとして保持。PDFファイル/画像/行矩形は変更していない。

- 左レール / 平面図 / 割当道具 / 台本の順へ変更。道具と台本が直接隣接し、DOMの読み順も一致。全体メニュー入口は左を保つ。狭幅は従来の一面表示と選択した項目の持越しを使用し、案内も実際の表示に合わせる。
- 現在頁によらず、局所fixtureの割当対象2件から「未割当」と「要付替え」を別集計。上部の警告→対象を見る→要対応のみ→対象を選択/ドラッグして割当、と続く。全件の位置割当後は警告を完了件数へ変更。Undo・版変更で再集計。台本パネルを畳んでも警告は残し、台本なしモードでは休止して対象データを保持する。
- assignment-warning-checks.json: 追加26項目成功。件数/改頁/折畳/Undo/改訂/狭幅/キーボード、道具と台本の隣接、操作途中で警告もデータも変わらないことを確認。
- assignment-dock-checks.json / assignment-dock-cancel-checks.json: 既存28+6項目を新配置で再実行して成功。JavaScriptエラー0。新旧を合算した重複込みの製品テスト総数ではなく、今回の局所操作確認60項目。
- assignment-warning-lint/report.md: 初回は390pxの補足文背景をPDF画像と判定してWARN 1件。補足領域のpaper背景を明示して再測定。最終390×844/1440×900はNG 0 / WARN 0 / 測定不可 0、最小5.03:1。assignment-warning-desktop.png / assignment-warning-mobile.pngで未割当と隣接配置を目視。PDF画像の文字はDOM測定外であり、前回の全頁目視確認を継承。
- node --check成功。基準の製品10ファイルはSHA-256一致。変更は資料内のみ。汎用PDF、保存/再読込、実iPad、本体統合、Fableレビューの既知の残課題は継続。

最新の確認先: http://127.0.0.1:8937/script-cues-2026-09-09/assignment-trial.html?v=2


## Qシートの表とCSV（2026-09-09）

本人からQシート単独の表表示・CSV形式の管理を希望。assignment-trial.htmlに左メニューのQシート入口とdialogを追加。q-sheet.js/css、q-sheet-csv.js、Q_SHEET_PLAN.mdを追加。変更前のHTML/JSはbefore-q-sheetとして保持した。

- 表はQ番号/種別/内容/きっかけ/担当/備考/台本位置。Qの追加、セル編集、上下/ドラッグ順序変更、編集候補をまとめて反映/破棄、CSV保存/読込み、台本位置へ戻る操作を用意。シーンはQ行へ自動変換しない。
- 局所state.cuesをQ定義の一か所にし、道具catalogを表示用に生成。番号/内容変更は安定cueIdと注釈を保持して台本ラベルへ反映。追加Qは警告の未割当に入る。表の反映を1回のUndoで戻せる。担当は文字メモで、共通人物/担当工程へは未接続。
- CSVはUTF-8 BOM、引用符/改行/カンマに対応。既知IDのみ更新、番号だけの自動結合なし。IDなし/未知IDは追加。CSVからの台本位置変更/既存Qの無言削除はしない。1MiB/200行、式風の文字を文字として出力し独自形式つきの再読込みで復元する。出力サイズも読込み上限内に制限する。Excel/Numbers実機での挙動は未確認。
- q-sheet-checks.json: ブラウザ操作27項目成功。表の編集/並び替え/反映/破棄、モーダル内フォーカス、実ダウンロードしたCSVの再読込み、部分CSV、不正CSV非適用、台本接続保持、番号連動、Undo、台本なし、スマホを確認。
- q-sheet-extra-checks.json: 6項目成功。低い横画面の編集可能な行/確定操作、番号重複の非適用、閉じた後のフォーカス、同一行の複数ラベルを確認。
- q-sheet-csv-checks.json: 純粋なCSV処理24項目成功。日本語・引用符・改行・式風文字・アポストロフィの往復、ID保持、欠落行の保持、不正入力各種、上限を検証。これを外部表計算ソフトの実機検証とは扱わない。
- 既存の割当28項目/警告26項目も再実行して成功。q-sheet-assignment-dock-checks.json / q-sheet-assignment-warning-checks.json。JavaScriptエラー0。構文検査成功。
- 画面検査: 標準design-lintのq-sheet-lintは、モーダルが意図的に覆ったinert背景のボタンをU4で37件NGと記録。該当する全件は背後の通常画面であり、操作可能なQシートの被覆ではない。共有lint/製品UIは変更せず、検査コピーのU4にinert祖先の除外1行だけを加えて有効な操作面を測定した。q-sheet-active-lintは390×844/1440×900でNG 0 / WARN 0 / 測定不可 0（最小5.94:1）、q-sheet-landscape-lintも844×390で同0件。コピーと変更理由はこの記録および各reportの備考に残す。
- 通常画面は標準の検査のままq-sheet-workspace-lintで390×844/1440×900、NG 0 / WARN 0 / 測定不可 0。q-sheet-desktop/mobile/landscape.pngを目視し、表の全列、内側スクロール、閉じる/反映への到達を確認。
- 基準の製品10ファイルを照合すると6件一致、stage-sketch.js/index.html/stage.html/style.cssの4件は前回基準から変化していた。このタスクの書込みは資料フォルダ内のみで、製品4件は編集も巻戻しもしていない。変更元は今回照合していない。試作は製品のJS/CSSへ接続しておらず、上記検証は独立した資料内のHTML/JSを対象とする。公開なし。PDF/画像/元の台本文字位置は不変。製品保存、共同編集、Excel/Numbers実機、実iPad、Fableレビューは未完了。

最新のQシート: http://127.0.0.1:8937/script-cues-2026-09-09/assignment-trial.html?v=3&sheet=open


## 実PDFの読込みとCSV移行（2026-09-09）

本人のPDF読込み・他アプリからの移行希望に対応。製品本体ではなく、既存の割当試作に実ファイルの入口を追加した。

- 左メニューからPDF選択→初頁プレビュー→新しい版として反映。50MiB/300頁、合計100MiB/8版。ローカルのPDF.js 5.6.205を同梱し、外部への送信・CDN依存なし。パスワード入力、エラー表示、取消あり。別頁は必要時に描画し、画像キャッシュ3頁、描画上限200万画素。元のFileと旧版はブラウザ内で保持する。
- PDFに取得可能な文字がある場合は本文断片、ない場合や余白は正規化した頁位置で割当。文字の区切りはPDFの情報に依存し、OCRや正確な単語分割を実装したとは扱わない。回転・CropBox付き頁も実PDFで確認した。既存PDF注釈のうちPDF.jsが描画できるものを参照表示し、Qへ自動変換しない。
- 改訂で旧版/安定cueId/注釈IDを保持し、以前の位置は要付替えに残す。頁数が違う版への切替とUndoで頁範囲を維持。Qシートから読み込んだPDFの位置へ戻る場合も、読込み完了を待って位置表示とフォーカスを戻す。GOとの接続はない。
- 汎用CSVは英語/日本語の列対応と全行候補を確認して新規Qを追加。未対応列・元種別・移行元ファイル名は備考に残す。番号だけで既存Qを上書きしない。番号が重なる場合は接頭辞で区別できる。UTF-8優先、読めなければShift_JISを試行し、対応画面に表示。従来の自社CSVによるID更新は保持。
- 公式調査: Stage WriteのDigital ScriptにはPDF出力と頁差替えの記載。Scriptationには注釈を含むPDF出力の記載。Stage WriteのCSV出力/独自形式の仕様は今回の公開情報から確認できていない。Louisの利用経験を現行全利用者の問題と一般化していない。参照URLと移行範囲はmigration-guide.html、PDF_IMPORT_MIGRATION_PLAN.mdに記録。
- pdf-migration-checks.json: 33項目成功（実PDF、初頁プレビュー、取消、文字/座標への割当、ドラッグ、旧版保持、回転/CropBox/3頁、短い版の頁範囲、Undo、パスワード、不正PDF、CSV列対応・未適用候補・番号衝突・表示幅）。pdf-migration-extra-checks.json: 17項目成功（読込途中の取消、スマホ/キーボード、Shift_JIS/任意見出し、未対応列保持、不正CSV、上限、式風文字の出力保護、読込失敗時の候補無効化）。pdf-q-jump-checks.json: 4項目成功（Q表から実PDF位置への移動・非同期表示後のフォーカス・位置スクロール・GOなし）。ブラウザのJSエラー0、PDF/CSVファイルの外部送信リクエスト0。検証PDFは架空の試験データで、競合の実出力ではない。
- 回帰: pdf-regression-q-sheet-checks.json 27項目、pdf-regression-q-sheet-assignment-warning-checks.json 26項目成功。今回は53項目の既存操作を再確認。前回の全テストを再実行したとの意味ではない。
- 構文検査: assignment-trial.js / pdf-import.js / q-sheet.js / csv-migration.js 成功。QA用の再現スクリプトはchecks/に保存。実行にはローカルサーバーとPlaywright、QA生成にはreportlab/pypdfを使用。
- 表示点検: pdf-workspace-lint、pdf-dialog-lint、csv-migration-lint、pdf-loaded-page-lint、migration-guide-lintはいずれも390×844/1440×900でNG 0/WARN 0/測定不可 0。PDFとCSVのdialog検査は、一時コピーのU4からinert祖先または最前面dialog外の背景要素だけを除外（共有ツールは変更なし、有効な操作対象は全て測定）。PDFの初回2件NGは閉じたdetails内部の再読込みボタンを検出したもの。折畳み内部の非表示をCSSでも明示して再測定。ガイドの初回1件NGは参照リンクが44px未満で、44pxへ修正。操作画面・PDFプレビュー・文字位置枠・注釈例・スマホ・モノクロの画像を目視確認。PDF画像内部の可読性はDOM自動測定に含まれない。
- 変更は資料フォルダ内のみ。製品10ファイルの当初基準は6一致/4変化（stage-sketch.js,index.html,stage.html,style.css）。別タスクの転換資料2件も基準から変化。いずれもこの作業では編集・巻戻ししていない。依存は資料内にコピーしただけで製品のpackageや保存形式は変更していない。
- 残り: 製品への保存/復元統合、競合の実出力による対応照合、縦書きなど多様な台本の実例、実iPad、独自形式の変換、OCR/自動再割当。局所試作は再読込みで初期化する。Fable外部レビューは未実行のまま。

最新の入口: http://127.0.0.1:8937/script-cues-2026-09-09/assignment-trial.html?v=4
移行ガイド: http://127.0.0.1:8937/script-cues-2026-09-09/migration-guide.html

## Stage Write実操作による検証（2026-09-09）

本人の体験開始・登録完了を受け、Stage Write Web版2.10.29、Pro体験版で架空の検証作品「Stage Sketch migration test 2026-09-09」を作成。製品側のコードは変更せず、migration-guide.htmlへ結果を追記した。

- fixture-script-original.pdf（架空台本2頁）をImport New PDFで読み込み、日本語の第1頁を表示。範囲のドラッグ→Right→文字入力でLQレイヤーに「LX 12 · 扉を開ける」を1件作成して保存した。実作品や会話画像は送っていない。
- Import New PDFの警告は、既存のActions / Light Cues / Texts / Chart associationsを除去すると明記。空の検証作品への初回取込みだけを実行しており、既存キューを消す再取込み試験はしていない。
- Replace Current Pageの案内は、選択PDFの先頭頁で現在頁を差し替え、注釈とChart linksは影響を受けないという内容。fixture-script-revised.pdfを選んで第1頁を差し替えたところ、LX 12の文字と枠が保持された。対象の台詞は改訂で下へ移ったがキューは旧位置に残った。範囲枠と文字枠のハンドルをそれぞれドラッグして移動・保存できた。
- Create PDF → Production - PDF → PRINT PDFでRecent PDFsに「Production - PDF 2026/9/9 12:28:50」とダウンロードアイコンが追加された。アイコンまで操作したが、内蔵ブラウザではダウンロードイベントも新しいPDFタブも得られず、出力ファイル取得は未完了。生成履歴とファイル内容の検証を分ける。出力PDFの日本語・注釈・再取込みや編集可能なQへの変換は検証済みとしない。
- UI操作中にコンソールエラーも観測したが、ダウンロード取得不能の原因とは断定しない。ファイル選択待ちのタイムアウトはツール側にもあり、初回はYESボタンのAX操作＋ファイル選択で取込みを完了。一般利用者にも取込み失敗が起きるとは判断しない。
- 未確認: ページをまたぐ再配置、複数頁一括改訂、舞台配置や経路との関連、CSV/独自形式出力、実iPad。他社の全体仕様やLouisの当時の操作・版は推測しない。検証作品はアカウントに残した。
- ガイドを既存ローカルHTTP経由で再読込みし、追記が提供されたことを確認。872pxと390pxで画像を目視し、文書横幅はそれぞれ857px/375pxでページ全体の横溢れなし。既存スタイルを使用した文章追記で、アプリ機能テストは再実行していない。新しいデザインlintの全項目合格を主張しない。

確認先: http://127.0.0.1:8937/script-cues-2026-09-09/migration-guide.html#stagewrite-trial

## Stage Writeの画面付きHTMLリファレンス（2026-09-09）

本人の「後でUIを確認するため、使い方をビジュアル化したHTMLで保存」の指定に対応。stagewrite-guide.htmlと画像内包のstagewrite-guide-offline.htmlを追加し、migration-guide.htmlからリンクした。対象は実際に試した台本・キューの操作で、全機能のマニュアルではない。

- 実画面8枚を撮影し、画面の見取り図、PDF取込み、範囲ドラッグとQ記入、編集ハンドル、改訂、PDF出力の6節へ整理。説明番号はHTML上の重ね合わせで、撮影画像は無加工。新規PDF取込みと1頁差替えの違い、差替え後の手動位置調整を模式図でも説明した。英語の実メニュー名を併記した。
- 撮影のため警告を開いて取消、既存Qの選択、範囲選択のプレビューを行った。新規Qや重複PDF出力は作成せず、検証作品を残した。以前確認できなかったPDFファイルの取得・出力内容・移行は未確認として明記している。
- CUAのスクリーンショット出力はJPEGデータだった。初期のpng拡張子の原本は保持し、バイト一致のjpgコピーを正規の参照先とした。manifest.jsonに8枚のハッシュと撮影内容を記録。単体HTMLは画像をdata:image/jpegとして内包し、約1MBで単独保存できる。
- 1440pxと390pxで文書の横溢れなし、主要操作対象の高さ44px以上、8画像の読込み、画像拡大・1440px等倍スクロール・Escapeとフォーカス復帰を確認。初回にTabが拡大ダイアログ外へ出たため、Tab/Shift+Tabの循環を修正して両幅で再確認した。初回結果を残し、guide-checks-final.jsonに修正後の結果を記録した。
- 単体HTMLの未表示画像はlazy loadingで初期読込みを待つため、各節へ移動して全8枚の表示を確認した。guide-static-checks.jsonで通常版/単体版のローカル参照先、IDの重複なし、画像8枚の元バイト一致、インラインJSの一致を確認。node --checkも成功。機内モードや実iPadでの確認とは扱わない。
- design-webの設計メモとトークンをSTAGEWRITE_GUIDE_DESIGN.mdに保存。本文・補助・強調の配色は個別測定で6.28:1以上。フルdesign-lintはCUA読み取り環境のCanvas/NodeFilter未提供により完了できず、合格とは記録しない。通常版のPC/スマホ画像と実画面を目視し、画像は元サイズで拡大できる。印刷CSSは用意したが実印刷は未確認。
- この変更は資料フォルダ内のみ。製品本体・他タスクの転換資料は編集せず、公開もしていない。次は生成済みPDFの実ファイル取得と移行後の照合が残る。

確認先: http://127.0.0.1:8937/script-cues-2026-09-09/stagewrite-guide.html
単体保存版: stagewrite-guide-offline.html

## Stage Write実出力PDFの取得・取込み照合（2026-09-09）

本人の「次の一手で」を受け、生成済みPDFの取得と舞台スケッチ試作への読込みを実施。前節までの「ファイル取得・出力内容未確認」は、以下の結果で更新する。

- 既存のProduction - PDF 2026/9/9 12:28:50からダウンロードアイコンを操作。「まもなくダウンロード開始」とポップアップ制限についての案内を確認した。CUAのdownloadイベントは返らなかったが、Downloadsの実ファイルを確認できた。イベントが返らないことを保存失敗と同一視しない。ブラウザ設定変更や再生成は行っていない。
- 元ファイル「Stage Sketch migration test 2026-09-09 - 2026_9_9 12_28_50.pdf」はDownloadsに残し、output/pdf/stagewrite-production-export-2026-09-09.pdfへバイト一致のコピーを保存。307,138バイト、PDF 1.4、2頁、SHA-256 df669b0b6642dc2532825adfa105447a5c2e7c32e0a893c8ea93e6a3f0603b22。
- pypdfで構造確認、Popplerで2頁を描画して目視。各頁に1928×2731の台本画像1枚があり、標準注釈は0件。元のfixture PDFは文字抽出可能で画像なしだった。出力PDFの台本本文は画像で、抽出文字はQ番号とページ番号などに限られる。Qは描画済みの文字と線であり、編集用のキュー構造はない。
- Stage Write編集画面では「LX 12 · 扉を開ける」が読めたが、実出力では「扉を開ける」が5つの四角となり、抽出文字はNULだった。原因となる設定や他の文字での再現性は検証していない。元台本の日本語本文は視覚的に読める。1頁は差替えた改訂版、2頁は残した旧版で、今回の1頁だけを差し替えた操作と一致する。
- 既存タブ10のassignment-trial.html?v=4でファイル選択→先頭頁確認→新しい版として反映。両頁を移動し、描画画像1187×1687の読込み完了と日本語本文を実画面で確認。LX 12と枠は同じ台詞の横に表示され、日本語Qの欠けも出力元と一致した。従って、この欠けは取込み後だけの現象ではない。
- Qシートは既存の1行「LX 12／照明を落とす」のまま。出力元の「LX 12／扉を開ける」がQへ変換されたものではない。PDFの本文選択候補はQの断片、読めないNUL、ページ番号で、台詞はない。画像主体なのに「文字の位置を選べる」と案内する点と、読めない候補を除く点は、次の改善候補として記録した。製品や試作の挙動は今回変更していない。
- stagewrite-guide.htmlに実PDFと取込み画面の比較、残る情報・失う情報、元の文字入り台本と別途Q一覧を用意する方針を追加。migration-guide.htmlの取得未確認表示を更新。単体HTMLも再生成（約1.7MB）、全ローカル画像を内包した。checks/build-stagewrite-offline.pyを再生成用に追加。変更前の3つのHTMLはbefore-stagewrite-pdf-verificationに保持。
- ガイドは872pxと390pxで全体の横溢れなし。狭幅の表は内部横スクロール。縦長のPDF画像を985px等倍で表示し、Escape後のフォーカス復帰を確認した。元の1440px固定指定を画像の実寸へ変更し、縦横比を維持。既存の配色・文字・余白トークンを使用し、フルdesign-lintや実iPad合格とは扱わない。
- 検証資料はstagewrite-guide-assets/pdf-export-structure.json、pdf-migration-verification.json、実PDFの2頁PNG、取込み後の2画面JPGに保存。試作は再読込みで初期化する。実アプリ本体、転換機能の共通契約、公開環境は変更していない。

確認先: http://127.0.0.1:8937/script-cues-2026-09-09/stagewrite-guide.html#migration-check

## 本文候補の品質判定と位置指定（2026-09-09）

Stage Write実出力を台詞候補として誤って見せないため、PDF取込み試作の本文候補を保守的に判定するよう変更した。製品本体・PDF原本・Q表のデータ形式は変更していない。

- 制御文字（NULを含む）と単独の頁番号を候補から除外し、除外後に可読な2行かつ12文字以上ある頁だけに本文選択を出す。条件を満たさない頁は候補を一切出さず、原因を画像化やフォントへ断定しない「位置指定で取り込むPDF」として扱う。
- 取込プレビューに候補数または除外数を出す。反映後の位置指定モードは、紙面を押して置く操作、44pxの「このページの中央へ置く」操作、キーボード時の案内を提供する。既存の文字PDFでは従来の折畳み本文選択を維持する。
- 実出力stagewrite-production-export-2026-09-09.pdf（2頁）で、取込プレビューに「読めない文字 5件・頁番号などの補助文字 1件を候補から除外」と表示され、反映後は本文選択が非表示、位置指定の操作だけが出ることを確認。LX 12を中央へ置くと「ページ内の位置（横50%・縦50%）」として割り当て、未割当が2件から1件になった。これはブラウザ内の試作用状態で、再読込み後は初期化される。
- fixture-script-original.pdf（2頁）では本文候補9行/90文字を表示し、本文選択にタイトル、役名、台詞、ト書きが出ることを確認。NULや頁番号はこの文字PDFの候補に存在しない。
- node --check pdf-import.js 成功。通常幅と390×844で位置指定画面を目視し、390pxではページ全体の横溢れなし、中央配置ボタン高さ44px、本文選択は非表示を測定。スクリーンショットでPC/狭幅とも位置指定の説明と操作が視認できることを確認した。全既存の自動回帰・実iPad・OCR・競合出力の網羅検証は再実行/実施していない。
- 変更はdocs/script-cues-2026-09-09/pdf-import.js、pdf-import.css、PDF_IMPORT_MIGRATION_PLAN.md、当記録のみ。公開なし。

確認先: http://127.0.0.1:8937/script-cues-2026-09-09/assignment-trial.html?v=4

## PDF・Q・位置アンカーの端末内復元（2026-09-09）

PDFを差し替えた後にも割当をやり直さずに済むかを確かめるため、局所試作にIndexedDBの単一ワークスペース記録を追加した。製品本体、生成済み`stage.html`、本体のproject schema、外部公開は変更していない。

- 記録はPDF File/Blob、PDF版IDと表示名、Q、typed subject、頁/矩形の正規化アンカー、表示状態を同一トランザクションで置き換える。ページ画像・PDF.js描画キャッシュ・Undo途中履歴・選択中の道具は保存しない。再読込みではPDF Blobを描き直してからQと割当を復元する。
- 端末内に保存中・保存済み・保存失敗・復元結果を件数の横へ常時表示する。保存不能時は画面上の作業を消さない。表示は「この端末・このブラウザ」の作業継続であり、バックアップ、他端末同期、製品保存とは記載しない。
- CUAで実出力`stagewrite-production-export-2026-09-09.pdf`を読込み、文字候補を使わない位置指定でLX 12を「横50%・縦50%」へ割当。再読込み後に、PDF 1版、LX 12のQ内容、1頁の位置アンカーが復元済みと画面で確認できた。
- Qシートの内容を一時的に「照明を落とす（保存確認）」へ変更して反映し、再読込み後も残ることを確認した。その後「照明を落とす」へ戻して保存し、最後の再読込みでも元の内容・PDF版・位置アンカーを確認した。検証用のQ変更は残していない。
- 390×844ではページ全体の横溢れなし、保存状態の表示あり、位置指定の中央配置ボタンは44px高。通常幅でも横溢れなし。`assignment-trial.js`、`pdf-import.js`、`q-sheet.js`、`script-cue-storage.js`の`node --check`は成功。
- 未確認: IndexedDB容量不足/破損時の実端末挙動、複数・大容量PDFの復元、実iPad、別ブラウザ/別端末移行、書出し、本体project schemaへの統合、競合アプリ実データの取込み。今回の検証はローカル試作だけで、これらを製品機能の検証と読み替えない。

確認先: http://127.0.0.1:8937/script-cues-2026-09-09/assignment-trial.html?v=5
