舞台スケッチの台本・キュー割当機能を、AstraとFableでブレストから設計計画へ進める依頼です。あなたはClaude Code Fableの独立設計レビュアー。編集、コマンド実行、ブラウザ、外部送信は禁止。次のファイルだけ読み、提案を日本語で返してください。新規セッションです。

Project: /Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/show-creative-ideas/shosai-app
読む範囲:
- docs/set-transitions-2026-09-08/OPERATIONS_2026-09-09.md（現在の担当・合図・途中待機の具体案）
- docs/set-transitions-2026-09-08/SPEC.md（転換設計、保存契約、時間の正本）
- stage-sketch.js の3732-3770,3900-3955,5690-5750,16485-16515行だけ（場面正規化、保存/Undo、現行明かり差分表）
- stage-sketch.jsの535-555行のexport関連、mcp-server/src/stage-model.jsのdocument validation関連は必要時に該当箇所だけ。
ライセンス/顧客/アカウント/認証/worker/他の作品原稿は対象外。

確認済み事実:
- ユーザーは「添付のように台本とキューを割り当てやすく、変更も問題なく行えるところまでUI/UXを大事に作り込む。まず設計計画」と依頼。添付はこのセッションで未受領。形式・実物・主端末は不明。推測で資料を読んだ扱いにしない。
- 現行明かりのキューシートは場面間の点灯・消灯・強度差分を印刷する。台本の行に呼び出し位置を付ける機能とは異なる。
- normalizeStateはprojectのキーを列挙再構成するため、新キーを追加するだけでは保全できない。内部v3、書出しv4、MCPはv3系の契約がある。
- 別の転換設計には内部cueIdと任意のQ番号、segmentsの合図待ち、同じ合図で待機中の複数対象を再開、早い合図の予約なし、試し進行だけの揮発状態、担当継続、旧rehearsalへの時間非転記がある。詳細は上記ファイルを優先。

Astraの初期仮説（反証・代案を歓迎）:
1. 主画面は台本本文＋行に並ぶキュー。選んだキューの詳細・舞台/転換への接続を右に出す。場面一覧・キュー表・全時間軸の常時4分割は避ける。
2. 「台本の箇所を選ぶ→照明/音響/転換などを付ける→意味と接続を確認」が最短経路。ドラッグ任意、クリック/タップ/キーボードでも変更できる。台本位置の移動とキュー順の変更は別操作。
3. スクリプト正本はテキストblock ID＋文字範囲＋短い前後文脈。表示ページ/行番号やcueの表示番号はIDにしない。外部PDF/Wordは原資料と版を保持。添付がPDFなら原PDFへピンを付ける経路が最短の可能性があり、テキスト化を確定しない。
4. 台詞の軽微な編集で位置は追従しても意味の確認は必要。削除/曖昧な改訂ではキューを消さず「位置を選び直す」。台本差替えは旧新比較、確かな対応/要確認/参照先なしを分け、曖昧な自動付替えを禁止。1操作Undo、保存状態と意味の確認状態は別。
5. 合図定義はプロジェクト共通の1正本にし、台本アンカーも転換手順も同じcueIdを参照する案。転換側のローカルcue配列は統合が必要。cue→transitionの逆一覧は派生にして二重正本を作らない。standby/GOを同一トリガーにしない。開始位置の合図と「前作業完了」の論理ANDは保持。
6. 最初の縦切りは架空台本6行/数キュー/既存案と同じ机・平台転換。「追加→位置移動→台詞改稿→参照欠落→付替え→Undo→印刷」で体験を検証。その後に実添付の入口と保存往復を固める。製品コード・公開は今回は対象外。

お願い:
まず方向性が異なる3案を出し、主軸1案を選ぶ（PDFへの注釈、構造化台本エディタ、キュー表/場面側起点など）。その後、Astra案の弱点・別案を具体的な失敗シナリオで検討し、実装可能な計画へ落とす。Astra案に合わせる必要はありません。
特に: 『割当』が台本↔キュー/キュー↔場面/キュー↔転換のどれか、キューの正体と部門別番号、台詞直後・動作中・同じ台詞の反復、1箇所複数キュー、台本更新時の負担、同一cueの複数箇所、タブレット、印刷と変更伝達、台本なし利用、転換側とID/確認状態/再生の衝突を考える。

出力:
1. 3案比較と推奨（添付未確認で決められない部分を明示）
2. 具体的な主画面と操作・状態（割当/変更/修復）
3. 転換側と合意すべき最小契約、反例、Astraへの異論
4. 段階計画と必要な受け入れ条件、最小の反証テスト
5. 本人への質問は重要な3つまで
ファイル事実、解釈、提案、未確認を分け、2000〜3500字程度。あなたの提案を採用決定や実装済み扱いにしない。
