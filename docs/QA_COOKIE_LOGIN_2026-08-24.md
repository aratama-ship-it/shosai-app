# クッキーログイン移行後の実機確認（2026-08-24）

デプロイ後に、この順で確認してほしい。**上から順に。前が通らないと次は意味がない。**

デプロイ:

```bash
cd "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/show-creative-ideas/shosai-app" && npx wrangler deploy
```

**必ず `cd` から実行すること。**「新しいWorkerを作りますか」「フレームワークは何ですか」と
聞かれたら、それはディレクトリが違う合図。中止する。

---

## 0. まず戻れることを確かめる（万一に備えて）

ログイン画面に不具合があっても、Basic認証は残してある。ここが通れば締め出されない。

```bash
curl -s -o /dev/null -w "%{http_code}\n" -u "利用者名:パスワード" https://shosai-app.juggler-arata.workers.dev/stage.html
```

**200 が返ること。** 返らなければ、それ以上進まずに報告してほしい。

## 1. PC（Mac）のブラウザ

| 確認 | 期待 |
|---|---|
| 公開URLを開く | ログイン画面が出る（緞帳の暗い背景に、紙が一枚） |
| わざと間違える | 「お名前かパスワードが違うようです。」が錆色の帯で出る。責める調子でない |
| 正しく入れる | 元のページへ入れる |
| **タブを閉じて開き直す** | **もう聞かれない** ← ここが今回の肝 |
| **ブラウザごと終了して開き直す** | **もう聞かれない** ← Basic認証では毎回聞かれていた |
| `/stage.html` を直接開く | ログイン後、`/stage.html` へ戻る（`/` ではなく） |
| `/sign-out` を開く | ログイン画面へ戻り、次は聞かれる |

## 2. iPhone / iPad の Safari

| 確認 | 期待 |
|---|---|
| ログイン画面の見た目 | 崩れない。文字が小さすぎない |
| 入力欄をタップ | **画面が拡大しない**（文字サイズ16pxで防いである） |
| パスワード管理（iCloudキーチェーン） | 保存の提案が出る。次回自動入力できる |
| 入って、Safariを完全終了、開き直す | **もう聞かれない** |

## 3. ★ホーム画面のPWA（今回の本命）

**先に、古い状態を消してから始める。**

1. ホーム画面の**アイコンを削除**
2. **設定 → Safari → 履歴とWebサイトデータを消去**
3. Safariで `/stage.html` を開き、ログイン画面から入る
4. ホーム画面に追加
5. **Safariを完全終了**（アプリスイッチャーから上へスワイプ）
6. ホーム画面のアイコンから起動

| 確認 | 期待 | これが今回直したかったこと |
|---|---|---|
| アイコンの絵 | 緞帳とスポットライト（「舞」の文字でない） | 8/23に解決済み |
| **Safariを終了した状態でPWAを起動** | **ログインを聞かれずに開く** | ★**Basic認証ではここで詰んでいた** |
| 聞かれた場合 | ログイン画面が出て、入れば以後は聞かれない | 少なくとも入る手段はある |
| 開いてから**10秒ほど待つ** | （裏でオフライン用の充填が走る） | |
| **機内モードにして再起動** | **開く** | オフラインの成立 |

## 4. 共有セッション（クッキー移行の影響確認）

セッションはWebSocketを使う。クッキー移行で壊れていないかを見る。

| 確認 | 期待 |
|---|---|
| セッションを開始して招待URLを発行 | できる |
| 別端末で招待URLを開く | ログイン画面 → 入ったあとセッションへ参加できる |
| 駒の移動・矢印が共有される | される |

## 5. Macアプリ

Macアプリは `shosai://app/index.html` をローカルから読むので認証を通らない。
**影響しないはずだが、念のため起動確認をしてほしい。**

---

## 見つけたら即報告してほしいもの

1. **手順0のcurlが200を返さない**（＝締め出されている。最優先）
2. ログイン画面から入れない、入っても弾かれる
3. ログインが**毎回**聞かれる（クッキーが残っていない＝移行の効果が出ていない）
4. セッションのWebSocketが繋がらない
5. 資料棚・名簿が**ログインなしで見える**（＝境界が壊れている。最重大）

## 私（Claude）が済ませた確認

- テスト485件全通過（うちクッキーログインの検査24件）
- 改竄・期限切れ・別口座の鍵・未知の利用者名・壊れた値 → すべて拒否することを確認
- 戻り先に `//evil.example` や制御文字を渡しても `/` へ落とすことを確認
- 資料棚・名簿・worker.js・wrangler.toml等が、クッキーもBasicも無ければ401であることを確認
- ログイン画面が外部リソースを一切参照しないことを確認
- ログイン画面をPC幅・スマホ幅・エラー時で描画確認

**確認できていないこと**: 実機のiOSでクッキーがアプリ再起動をまたいで残るか。
これは実機でしか分からない。**今回の移行の成否はそこにかかっている。**
