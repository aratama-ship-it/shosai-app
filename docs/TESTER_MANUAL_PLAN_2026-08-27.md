# テスター向けマニュアル 計画書（構成案・掲載リスト）

作成 2026-08-27・Claude。**まだ構成案の段階。本文は未執筆。**
このファイル単体で引き継げるよう、前提と出典を明記する。

## 前提（出典つき）

- 配るのは **舞台スケッチ単独版 `stage.html`**。書斎（`index.html`）は身内用で配らない
  （[P1-6_feature-reachability-table_2026-08-23.md](P1-6_feature-reachability-table_2026-08-23.md) の「二つの製品の区別」）。
- リリース形態は **β版・限られた相手のみ** と本人確定
  （[RELEASE_DECISION_2026-08-26.md](RELEASE_DECISION_2026-08-26.md) 末尾の回答表）。
- 入口は `https://shosai-app.juggler-arata.workers.dev/stage.html`。
  テスターごとに **名前＋パスワード**（`GUEST_ACCOUNTS`、発注書J）でログイン。
- 端末差は**欠落ではなく確定仕様**：iPadに舞台機構・3Dカメラは足さない、楽曲はPC版のみ、
  スマホは閲覧機モード（メモリ `ipad-missing-features-are-intentional` ほか）。
- **不具合の連絡経路は未決**（IDEA_BACKLOG_2026-08-27.md B-3）。マニュアル完成までに要決定。
- 想定読者は舞台・パフォーマンス関係の招待テスター。**技術者ではない**前提で書く。

## マニュアルの狙い（design-policy.md の5項目に沿った整理）

1. **誰が・どの場面で**: 招かれたテスターが、自宅や稽古場で自分の演目を1シーン描いてみる。
   または本人のセッションにゲスト参加する。
2. **どんな気分に**: 「説明書を読まされた」ではなく「舞台の道具を渡された」。
   読み終える頃には1シーン描けている構成にする。
3. **固有の核**: 舞台の語彙で書く（上手・下手、転換、盆、袖、板付き）。
   汎用アプリマニュアルの言い回し（「〜機能をタップ」）に寄せない。
4. **踏襲と外し**: 踏襲＝冒頭クイックスタート・末尾Q&A。
   外す＝全機能辞書を先頭に置く構成（リファレンスは後半へ）。
5. **機能条件**: 3端末で読める／βの更新に追従しやすい形式／既知の制限を正直に書く。

## 掲載する機能リスト（端末別の可否つき）

「PC」=PCブラウザ、「iPad」=stage.htmlをホーム画面に追加したPWA、「スマホ」=閲覧機モード。
正: `stage-sketch.js` の `PANELS` 定数＋P1-6到達表＋2026-08-25までの確定仕様。

| 機能 | PC | iPad | スマホ | 備考 |
|---|---|---|---|---|
| ログイン（名前＋パスワード） | ○ | ○ | ○ | 端末ごとに1回 |
| ショー（プロジェクト設定） | ○ | ○ | △ | スマホは表示＋書き出し中心 |
| シーン一覧（追加・削除・地図・セクション） | ○ | ○ | △ | スマホは前後送りのみ |
| 正面図／平面図／両方表示 | ○ | △ | △ | iPad・スマホは単一ビュー切替 |
| 出るもの（演者・舞台セット・小道具） | ○ | ○ | ✕ | |
| ポーズ・動きの矢印・付箋 | ○ | ○ | ✕ | 矢印は正面図（平面図対応は今後） |
| 選んだもの（インスペクタ） | ○ | ○ | ✕ | |
| セットを組む（3Dモデル編集）・セット登録 | ○ | ○ | ✕ | |
| 舞台機構（状態・転換再生・盆） | ○ | ✕ | ✕ | **意図的にPCのみ** |
| 3Dカメラ | ○ | ✕ | ✕ | **意図的にPCのみ** |
| 照明・光の意図カード | ○ | ○ | ✕ | 重ね表示は書く側の道具 |
| 背景 | ○ | ○ | ✕ | |
| 音楽 | ○ | ✕ | ✕ | **楽曲はPC版のみ（確定）** |
| 場面問答／シーン研究 | ○ | ○ | ✕ | |
| 劇場プリセット（シアタートラム等）・劇場取り込み | ○ | ○ | ✕ | |
| 保存・書き出し・読込・バックアップ・Undo | ○ | ○ | △ | スマホは「ショー」パネルから書き出し |
| 共有セッション（ゲスト参加） | ○ | ○ | ○ | スマホは見る係 |
| オフライン起動（PWA） | − | ○ | − | stage.htmlのみ。書斎側は不可 |
| 端末による違い一覧（アプリ内の表） | ○ | ○ | ✕ | スマホに入口なし（確定させるか未決） |

**載せないもの**: AI指示（Macアプリ専用。ブラウザには存在しない）、書斎の機能一式
（棚・名簿・制作机。別製品）、ホスト側のセッション運営の詳細（テスターはゲスト側だけでよい）。

## 構成案（10章）

前半＝手を動かす順、後半＝引く順。「読む→試す→困ったら戻る」の二段構え。

| 章 | 内容 | 狙い |
|---|---|---|
| 0 | 表紙・はじめに | β版であること・連絡先を最初に。1/3ページ |
| 1 | 舞台スケッチとは | 「舞台の場面を、正面と真上から描く道具」。3行＋図1枚 |
| 2 | 入り口 | URL・ログイン・**端末ごとの推奨の開き方**（iPadはホーム画面追加の手順を写真つきで） |
| 3 | まず1シーン描く | 10分チュートリアル。ショー作成→劇場を選ぶ→演者を置く→動きの矢印→保存確認まで一本道 |
| 4 | 画面の地図 | 正面/平面/両方、パネルの並び。スクリーンショット1枚に引き出し線 |
| 5 | 機能ガイド | パネル別リファレンス（上のリスト順）。各機能=見出し＋2〜4行＋画像。読み物にしない |
| 6 | 共有セッション | 招かれたときの入り方・「操作中」バナーの意味・切れたときの復帰（最新を取り直す） |
| 7 | 端末による違い | 上の表の読者向け版。**「欠落ではなく設計」を明記**し、問い合わせを減らす |
| 8 | データはあなたの端末の中 | 保存の性質（ブラウザ内保存）・ブラウザのデータ削除で消えること・書き出しでの控えの取り方 |
| 9 | 困ったとき Q&A | ログインできない／オフラインで開かない／音楽が見当たらない（=PC専用）／読込で戻す 等 |
| 10 | β版のお願い | 既知の制限・報告のしかた（**連絡経路が決まり次第記入**）・報告テンプレート（端末・画面・再現手順） |

- 8章は独立させる。テスターのデータ消失は信頼を最も損なう事故で、
  localStorage逼迫（P1-12）がリリース後送りである以上、控え（書き出し）の習慣づけが保険になる。
- チュートリアル（3章）はPC前提で書き、iPad差分は欄外注記にする。二本立てにしない。

## 決めること（本人判断・3件）

1. **配布形式**。✅ **2026-08-27 本人確定**: 推奨案どおり、ログインの内側に `manual.html` を
   置いて舞台スケッチからリンク＋配布時にPDFを1部添える（Web正・PDF従）。
   PDFは `manual.html` の印刷用CSSからの書き出しで作り、二重管理にしない。
2. **不具合の連絡経路**（B-3）。✅ **2026-08-27 本人承認**: アプリ実装済みの
   〈感想を送る〉→ Googleフォーム（`stage-sketch.js` の `FEEDBACK_URL`）を正式な連絡経路とする。
   マニュアル第10章はこのボタンを案内する形で執筆済み。IDEA_BACKLOG の B-3 は解決。
3. **テスターの顔ぶれと想定端末**。**未決。** PC無しのテスターがいるなら、iPad章の厚みを増やし
   「PCのみ」機能の扱い（触れない/短く紹介）を変える。当面はPC前提＋iPad欄外注記で執筆。

## アプリ内「使い方検索」（2026-08-27 本人要望で追加）

「アプリ内にも使い方検索みたいなものを入れて、見られるようにしたい」との要望。
マニュアルと検索結果の食い違い（二重管理）を避けるため、次の一元管理構成を正本とする。

- **本文は構造化データ1本**（`manual-content.js` 想定）。項目ごとに
  `id / 章 / 見出し / 本文HTML / キーワード（言い換え語）` を持つ。
- **`manual.html`** はこのデータを章順に並べて描画する読み物（印刷CSS→PDF書き出しもここから）。
- **アプリ内検索**は同じデータを検索窓で引く軽いオーバーレイ。結果は短い答え＋
  「くわしくはマニュアルの該当章へ」リンク（`manual.html#anchor`）。
- **検索の質はキーワード欄で決まる。** テスターは正式名称で引かない。
  「音が出ない→音楽はPC専用」「消えた→保存・書き出し」「回る→盆」のような
  日常語・症状語の言い換えを各項目に持たせる。
- 実装の分担: 本文データと `manual.html` はClaudeが書く。アプリ内オーバーレイ
  （`stage.html` 側の変更）は仕様確定後にCodexへ発注（別紙発注書。キャッシュ版上げ・
  配信許可リスト・回帰テストを含めること）。**アプリ本体の変更は本人承認後に着手。**

## 進め方（決定後）

1. 上の3件が決まったら本文を執筆（章順は3→2→8→7→5→…と、手を動かす章から）。
2. スクリーンショットは実機で撮る（推論で描かない。メモリ `verify-on-real-device-not-by-reasoning` と同じ理由）。
3. `manual.html` にする場合はデザイン方針（`_claude-rules/design-policy.md`）を適用し、
   配信に含める際は `.assetsignore`・キャッシュ版上げ（`?v=`・`CACHE_NAME`）を確認する。

---

## 実施記録（2026-08-27・Claude）

**初版ドラフトを作成した。** 置き場所は `shosai-app/manual/`（まだ配信には載せていない）。

- [`manual/manual-content.js`](../manual/manual-content.js) — **本文の正本**（全10章35節＋検索用の言い換え語）。
  冊子とアプリ内検索の両方がこの1ファイルを読む。本文を直すのはここだけ。
- [`manual/manual.html`](../manual/manual.html) — 読む用の冊子。アプリと同じ「暗い机の上の紙」の視覚言語
  （本体 `:root` の色・書体トークンを複製）。右小口に辞書の爪風の章タブ、紙面上部に検索窓、
  印刷CSSあり（ブラウザ印刷→PDFが配布用PDFになる）。

**執筆中に分かったこと（計画の前提が2つ動いた）:**

1. **アプリに9段の実践ツアーが既にある**（初回自動・〈設定〉→〈使い方〉で再表示。
   `stage-sketch.js` の `TOUR` 定数）。チュートリアル章はこれと重複させず、
   「内蔵の案内が最初の先生」＋その後に試す3つ、という構成に変えた。
2. **〈感想を送る〉ボタンとGoogleフォームが実装済み**（`stage-sketch.js:20703` `FEEDBACK_URL` 設定済み）。
   未決だった連絡経路（B-3）は実質これで決まっている可能性が高い。
   **本人確認1点: このフォームをβの正式な連絡経路とするか。** マニュアル第10章はこのボタンを案内する形で執筆済み。

**動作確認（ローカル）**: 検索（言い換え語ヒット・0件時の案内・noindex）、章タブ、
端末別の表、問答形式のQ&A、日付の差し込みを確認。プレビューは
`python3 -m http.server 8765 -d /tmp/shosai-preview`（iCloud直下はプレビュー用サーバーの
プロセスから読めないため /tmp へ複製して配信。正本はあくまで `shosai-app/manual/`）。

**残作業:**

| 作業 | 中身 | 担当の目安 |
|---|---|---|
| 図版 | 実機スクリーンショットの撮影と差し込み（iPadホーム画面追加の手順は実機必須） | 本人＋Claude |
| 場面問答の掲載可否 | stage.html 単独版に study パネルが見当たらず未掲載。実機で確認してから判断 | 本人QA |
| アプリからのリンク | ✅ 実装・検証済み（[発注書N・使い方検索](WORKORDER_N_MANUAL_HELP_2026-08-27.md)。Codex実装・Claude検証。commit `2f21b1a`） | 済 |
| アプリ内「使い方をさがす」 | ✅ 同上。実ブラウザで「消えた」→2節ヒット・0件文言・Esc閉じを確認。テスト616件通過 | 済 |
| 配信への組み込み | ✅ `.assetsignore` 変更不要を確認。APP_SHELL＋CACHE_NAME v154・各?v=上げ済み。**デプロイはまだ**（本人確認待ち） | デプロイ待ち |
| PDF書き出し | ✅ `manual/使いかたの冊子_2026-08-27.pdf`（印刷CSSから生成）。再生成: ローカル配信して headless Chrome の `--print-to-pdf` | 済 |
