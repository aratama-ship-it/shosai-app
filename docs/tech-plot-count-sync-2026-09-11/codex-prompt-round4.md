第4ラウンド（短く・1,300字以内）。本人回答: Q1=床上の移動だけでよい。Q2=**両方**（一人の演出確認も、複数端末で同じ瞬間を見る稽古も）。Q3=代表配置は場面ごとに選べるように（主にサムネイル用途、開いた時の初期表示にも使う）。

依頼:
1. Q2=両方を受けて、共有セッションでの「場面内再生」の最小契約を決めたい。既存の共有セッションは場面単位で同期し、ホスト（Mac側）とゲスト端末がある。案: (i) 再生位置（場面内カウント・再生中フラグ）をホストだけが送り、ゲストは受信して同じ配置を描く（音源はホストだけ再生、ゲストは無音でも可）／(ii) 各端末が同じ音源を持ち時刻同期／(iii) 初版は単端末のみ、共有は「現在カウント」の静止同期だけ（再生はしない）。推奨と理由、各端末に音源が無い場合の扱い、ゲストが勝手に動かさない仕組み。
2. Q3の「代表カウント」フィールド案（例: motion.heroCount）と、サムネイル・初期表示・香盤表・演者ビューアでの使い分け。
3. Q1=床上のみで、既存の「降りる→歩く→登る」3段と base（高さ）を場面内キーフレームでどう扱うか（保持？固定？）。
4. 以上を踏まえ、段階⑦⑨の試作範囲を1文で再定義。
