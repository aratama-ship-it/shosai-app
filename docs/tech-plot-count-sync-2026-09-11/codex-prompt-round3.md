第3ラウンド。本人の回答が出ました。
- K1: 推奨どおり（作らない・受け取る・薄く繋ぐ）。
- K2: 順序はよく分からないが、最終的にその方向で使えればよい。
- K3: (c) 別の理由。「当時は時間的な動きより客席からの見え方を重視したから」。ただし**場面内部の細かい動きをよりミクロに作るのは良い**ので、後々作りたいものとして「タイムライン式」「ダンスフォーメーション」の機能はあった方がよい、と明言。
- K4: 必要ならどちらでもよい（保存項目の追加は可）。

つまり「タイムライン不採用」は原理ではなく当時の優先順位でした。前提が変わったので、計画の後段を組み直したい。
追加の事実（コード確認済み）: 場面間の転換は beginSceneAnim が同一idの駒を前場面→次場面へ直線補間（u,v）し、演者は「降りる→歩く→登る」の3段、光の強さ・ビーム・機構値も補間、はける駒は薄くなって消える。経路は直線のみ。cueSeconds は行き先の場面が持つ。共有セッション・FPV・演者ビューアは「場面」単位で同期。

依頼（全体2,000字以内・見出し付き）:
1. 「場面内のミクロな動き（カウント同期のフォーメーション）」の設計選択肢を3つ比較: (A) 場面の細分化＝子場面に到達カウントを持たせ、既存の場面間補間をそのまま使う／(B) 場面内キーフレーム＝1場面の中に count で並ぶ配置を複数持つ新構造／(C) 別モード「フォーメーション譜」。既存の sceneAnim・共有セッション・FPV・演者ビューア・香盤表への波及、保存形式、Undo、iPadでの操作、の観点で。推奨と理由。
2. 推奨案での最小データモデル（フィールド名例）と、前回の「音楽の錨（countBpm/firstCountSec/arrivalCount）」との接続。錨→自動送りは、この文脈では「場面内の再生」として何を意味するか。
3. UI: 横長タイムラインを作ってよい前提で、それでも舞台スケッチらしい入口（客席からの見え方が主、時間は従）を保つ案。Choreographic の複製にならない差別化点を2つ。
4. 段階の組み直し: ①映像面 ②ヒアリング ③音楽の錨 ④占有駒 ⑤参照/書き出し ⑥MVR に、⑦場面内キーフレーム ⑧経路（曲線・多点）⑨場面内再生 をどこへ入れるか。各S/M/L。
5. 本人へ次に聞くべき質問を最大3つ（答えで設計が変わるものだけ）。
