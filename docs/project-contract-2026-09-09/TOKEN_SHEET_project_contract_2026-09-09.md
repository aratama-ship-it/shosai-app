# project workflow 契約資料のトークン

2026-09-09 / 静的な判断資料。実アプリの新規トークンではない。

| 区分 | 値 | 用途 |
| --- | --- | --- |
| paper | `#efe7d6` | ページ背景 |
| page | `#fffaf0` | 表、要点、カード |
| ink | `#2b2620` | 本文、見出し |
| muted | `#6a604e` | 補足 |
| accent | `#8f3e1e` | 判断点、リンク、状態 |
| desk | `#201b16` | 関係図の背景 |
| line | `#75664f` | 罫線 |
| font | `"Hiragino Sans", "Yu Gothic", system-ui, sans-serif` | 日本語本文 |
| heading | 32 / 24 / 18px、行高1.5 | h1 / h2 / h3 |
| body / small | 16 / 14px、行高1.75 | 本文 / 補足 |
| spacing | 4 / 8 / 12 / 16 / 24 / 32 / 48px | 余白単位 |
| touch | 44px | リンクとsummaryの最小操作高 |
| radius | 4px | 控えめな状態面 |
| motion | なし、reduced-motionでも同じ | 静的な判断資料 |
| Q timeline sidebar | 幅440px、最大高920pxまたは`100dvh - 40px` | 舞台スケッチ右パネルに収まる縦長の読み取り専用ビュー。中段だけスクロールする。 |
| timeline rail | 30px / 1px線 / 12px点 | Qの順序を連続して示し、選択したQだけ詳細を展開する。 |
| type colors | LX `#8f3e1e` / 音響 `#315a70` / 転換 `#4e5e38` / 進行 `#644565` | 色はQ種別の補助。Q番号と種別を常に併記する。 |
| detail / touch | 15px本文、12px補足、操作44px | 台本は頁番号と引用断片の参照に縮め、activity・担当・所要・条件を選択Qの直下に出す。 |
| delivery sidebar | 幅440px、最大高920pxまたは`100dvh - 40px` | 現場の書き出し先を選ぶ右パネル。選択先と固定の書き出し操作を残し、中段だけをスクロールする。 |
| recipient row | 最小高56px、左線3px、内余白10px | 全体・部門・個人を一つのリストに置き、選択先の役割をラベルと補足で判別する。 |
| delivery relation | 操作 `#8f3e1e` / 前後参照 `#6a604e` | 各Qを「その人の操作」か「前後参照」か、文字と線で示す。色だけに依存しない。 |
| delivery action | 44px、2列、間隔8px | 印刷用HTMLとCSV保存を、選択先の下で同じ位置に固定する。 |
| receiver web view | 最大幅1320px、最大高920px、右ペイン440px | 受け手の役割と現在Qを切り替え、舞台状況を左、本人の操作と前後Qを右に同居させる。 |
| stage map | 最小高320px、4列グリッド、前方ラベル20px | 架空fixtureのA・B・机の位置だけを表示する。実舞台・カメラ・位置追跡を表すものではない。 |
| receiver control | 44px、役割selectと前後Qボタン | Q切替は閲覧用で、GO・共有・実行済みの操作には接続しない。 |
| check row | 最小高44px、操作色`#8f3e1e`、未完`#6a604e` | 確認欄は画面内だけで保持し、切替で初期化する。現場の実行記録として保存しない。 |
| receiver memo | 本文14px、最小高112px、保存操作44px | シーンメモは舞台状況の直下、QメモはQシートの直下に置く。見る人・シーン・Qで分け、fixtureではこのブラウザだけへ保存する。 |
| stage markup | 矢印3px、付箋12px / 行高1.5、道具・消去44px | 矢印はドラッグ、付箋は配置先クリック。見る人・シーンごとのレイヤーに置き、人物・大道具の正本配置を変更しない。 |
| collaboration preview | 舞台主表示: 最小高360px / 右パネル: 300px / 間隔16px | 役割ごとの見え方と公開範囲を同じ画面で照合するfixture。小画面では舞台を先、公開範囲を後ろへ縦積みにする。 |
| visibility choice | 最小高44px / 選択状態: 左線3px + 太字 / 状態文11px | `private`・`department`・`team`は色だけで区別せず、範囲名と閲覧者を同時に表示する。確認前には送信・共有済みの表現を使わない。 |
| chief aggregation | 音響 `#315a70` / 照明 `#644565` / 制作 `#8f3e1e`、矢印3px・見出し12px | チーフには音響・照明の部門共有と制作チーム共有を同じ一覧へ出す。部門名・範囲・種別を文字で併記し、privateは集約しない。 |

コントラストの実測（sRGB相対輝度）: ink/paper `12.19:1`、accent/paper `5.94:1`、ink/page `14.41:1`、muted/page `5.94:1`。Q種別の文字色は page 背景に対して LX `7.02:1`、音響 `7.14:1`、転換 `6.77:1`、進行 `7.82:1`。本文と状態のいずれもAA基準を満たす。狭幅680px以下では2列を1列へ、幅の大きい関係図は図だけを横スクロールにする。
