# W2 隔離生成DB差分

## 結論

現在の `db.js` は、日付表示を除けば現在の正本から隔離再生成した出力と完全一致した。データ内容を理由とする `db.js` の置換やキャッシュ版数更新は、このレビューでは不要である。

| 項目 | 現行 | 隔離生成 |
| --- | ---: | ---: |
| 生成日 | 2026-08-23 | 2026-08-28 |
| バイト数 | 15,508,152 | 15,508,152 |
| works / elements / persons | 2,663 / 742 / 1,331 | 2,663 / 742 / 1,331 |
| 差分のあるトップレベルキー | `generated` のみ | `generated` のみ |
| `generated` 除外後の完全一致 | はい | はい |

## 安全境界

- `build_db.py` は `/tmp` の使い捨てコピーでだけ実行した。
- コピー側では `index.html` の `db.js?v=81` が `v=82` へ進んだが、正本の `index.html` は変更していない。
- 隔離 `db.js` は `node --check` を通過した。
- 正本・既存 `db.js`・アプリファイルの17保護ハッシュは波の後も一致した。

構造化した測定値は [w2-isolated-db-diff.json](w2-isolated-db-diff.json) にある。
