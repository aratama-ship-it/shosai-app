# 夜間ラン報告：全ショー 小道具・グループ表示／香盤表 描画エラー一括スキャン（2026-08-20）

実行 03:34–04:40 JST。Codexが実装しClaudeが検証。**本体アプリとショーJSONは一切変更していない。**

## 結論（3行）

- **致命 0件 / 描画エラー 0件 / 香盤表の異常 0件**（12通り × 32ショー ＝ 384回の読み込み・全シーン描画・印刷生成）
- 種を変えた反復3周（seed 43/44/45）でも **0件**
- 別件で **既存チェッカに許可リスト外の6件**（`継ぎ目の庭` r1）。**退行ではなく未レビュー範囲**。本人判断待ち

## 実施内容

| 波 | 内容 |
|---|---|
| 波A | 現存32ショーをそのまま読み込む退行検査。小道具コードが既存表示を壊していないか |
| 波B | 形11種の小道具をメモリ上だけで合成注入し、右手・左手・床・舞台外・受け渡し・はけの6状態を作って検査 |

組み合わせ12通り＝ 波(A/B) × 言語(ja/en) × 画面幅(1440x900 / 1024x768 / 390x844)。

対象32本の内訳: `.stage-sketch-mcp/projects` 6、`.stage-sketch-mcp/exports` 8、`jjk-show` 4、
`show-creation` 1、`show-creation/demo-11works-2026-08-16/sketches` 13。**対象外0本**（全て読めた）。

検査した版: `stage-sketch.js?v=263` / `CACHE_NAME stage-sketch-pwa-v94` / Chrome 151.0.7922.140 / Node v22.17.0。

## 検証（Claudeが自分で実行した分）

Codexの「できました」を鵜呑みにせず、次を自分で確認した。

1. **テスト**: `cd tests && node --test index.mjs` → **341/341通過**（Codex完了後の最終版で再走）
2. **制約監査**: `Math.random` 0件、`writeFile` はレポート出力の1箇所のみ、形・DOM idの写経0件
   （`PROP_SHAPES`・`STORAGE_KEY`・`LANG_KEY`・DOM idはすべて実行時に `stage-sketch.js` から取得）
3. **注入が効いている証拠**: `synthesizeProps` で形11種→登録11件、6状態すべて到達
   （`right/left/floor/offstage/handoff/disappears` すべて true）。原本の `sets` 件数は不変
4. **検出が発火する証拠（故意破壊）**: 写しに参照切れ `heldBy` と同じ手の重複を仕込み、
   **E1・E2がそれぞれ正しく発火**することを確認
5. **空回りでない証拠**: prop登録2件の写しで全シーンD3（各シーンの小道具行数＝2）が一度も鳴らない
   ＝画面に実際に2行描かれたことを検査が確認している
6. **香盤表の実物確認**: 60行（実シーン50＋セクション10）のショーで印刷HTMLを保存し、
   **列51＝実シーン50+1・行11＝小道具11形・不正セル0**（`undefined`/`null`/`NaN`/`[object Object]` なし）を独立に数えた
   → [sample-print.html](sample-print.html)
7. **保護対象**: 本体・ショーJSON 39ファイルのSHA-256を実行前後で照合し **不一致0**

### 検証中に私が誤った点（記録）

- 「60シーンのショーで香盤表が51列しかない＝バグ」と一度判断したが誤り。
  `sceneRows()` は `kind:"section"`（章見出し）を除外しており、実シーンは50。**列51は正しい値**。
- 最初の制約監査はCodexがまだ編集中の版に対して行っていた。最終版で再監査済み（上記2）。

## 検出（本人の判断が要るもの）

### 1. `check-object-on-performer` の許可リスト外6件（R2）

`.stage-sketch-mcp/exports/継ぎ目の庭_—_和とサーカス60分ストーリーショー-舞台スケッチ_v1-r1.json`

- seam-scene-1-2: sphere「記録球3」が 保管人 の上（1.67m）
- seam-scene-2-1: block「空白の記録紙（位置札）」が 保管人 の上（1.43m）
- seam-scene-8-2: suitcase「記録用スーツケース」／block「空白の記録紙（位置札）」が 保管人 の上（1.43m）
- seam-scene-8-3: wall「枠・左辺」が 保管人 の上（2.04m）／wall「枠・右辺」が こぼれ の上（1.98m）

**退行ではないと判断した根拠**（事実）:
- 許可リスト27件は別6ファイル（航路図・スタンドイン・落ちる練習・ルールブック等）由来で、
  `継ぎ目の庭` の項目は**1件も無い**＝2026-08-20の40件レビューの対象範囲外だった
- 当該 r1 は **2026-08-08 以降未更新**（レビューより前のまま）
- **新しい r2（08-19 20:32）では検出0件**

**推測（未確認）**: r1は r2 に置き換えられた旧版で、6件は旧版に残った表現／データ。
**提案**: r1を対象から外すか、6件を「意図した表現」として許可リストへ入れるかは本人判断。**今回は何もしていない。**

### 2. レポートの軽微な不備（スキャナ側）

- `preflight WARN: なし` と出るが、実際のpreflight結果と連動していない（今回は実際にはWARN 1件あり）。
  固定文言なので、次に触るときに実結果を渡すか、項目自体を削るのが妥当。

## 承知の上で開始したWARN

preflight（03:42, `--profile generic`）: FAIL 0 / WARN 1 / PASS 2。
**キャッシュ以外の同期競合コピー4件**（先頭 `pygmix/PYGMIX/90_legacy/passed/pygmy ojisan_sit 3.ai`）。
本スキャンの対象外。**削除も移動もしていない。** 棚卸しは `_maintenance/icloud-conflict-cleanup/`。

## 途中で起きた事象

- 03:44頃、**並行セッションが本体を v262→v263（CACHE v93→v94）へ更新**（受け渡し線の追加）。
  当初のbaselineと差分が出たため一度停止し、出所を特定のうえ本人承認を得て再開。
  `stage.html` の再生成も並行セッション側で実施済みだったため、私は重複実行していない。
  baselineは `baseline-sha256-v263.txt` として取り直した。
- 04:13:47に本走は完了していたが、私の監視ループが自分自身のコマンド文字列に一致して
  「実行中」と誤検出し、約7分空回りした（結果には影響なし）。

## 成果物

- `tools/scan-prop-render.mjs` … スキャナ本体（読み取り専用）
- `tools/scan-prop-render.README.md` … 使い方・検出項目
- `tests/stage-prop-render-scan.test.mjs` … 純関数の単体テスト
- 本ディレクトリ: 組み合わせ別レポート12枚＋`report-summary.md`＋seed別3枚＋`sample-print.html`

## 再実行のしかた

```
cd shosai-app
node tools/scan-prop-render.mjs --matrix
```

## 並行セッションとの重複（要調整）

04:20〜04:24に、**別のClaudeセッションが同じ予備キュー（R1〜R4）を並行実行**していることを確認した。
このディレクトリに、私が作っていない次のファイルが生成されている。

- `night-loop.sh`（07:00まで種を変えて反復するループ。**04:40時点でスキャナ3プロセスが稼働中**）
- `seeds/seed43-*.md`、`r2-object-on-performer.txt`、`sample-print-50scenes.html`

いずれも読み取り専用スキャンの出力で、本体・ショーJSONへの書き込みはない（保護対象39ファイルの照合は不一致0）。
破壊的な競合ではないが、**同じ作業を2セッションが二重に回している**。

- 私は他セッションのループを停止していない（07:00に自動終了する設計のため）
- 私の成果物は `REPORT_2026-08-20.md`・`report-*.md`（12枚＋seed43/44/45）・`sample-print.html`
- 他セッションの成果物は `seeds/` 配下・`sample-print-50scenes.html`・`r2-object-on-performer.txt`

**朝の判断事項**: 夜間タスクの起票を1セッションに寄せるか、PLAN.mdに担当セッション名を書く運用にするか。
