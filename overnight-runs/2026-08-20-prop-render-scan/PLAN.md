# 夜間ラン計画：全ショー 小道具・グループ表示 描画エラー一括スキャン（2026-08-20）

開始 03:34 JST / 終了目標 07:30 JST（最終検証は 07:00 開始。07:00以降は新規waveを始めない）
本人不在・判断不要の自動完走タスク。**本体アプリとショーJSONは変更しない読み取り専用スキャン。**

## preflight
`--profile generic` で FAIL 0 / WARN 1 / PASS 2。
承知で開始したWARN: **同期競合コピー 15件**（`show-reference/obsidian/` 等。本スキャンの対象外・
削除せず本人へ報告のみ）。

## 前提として確認した事実（Claudeが実測）
- 現存ショー32本の `kind:"prop"` 登録は **全て0件**、`heldBy` も **0件**。
  小道具機能（2026-08-20実装）より前に作られたため。
  → 素直な読み込みスキャンでは新コードをほぼ通らないので、**小道具を合成注入する波Bを本命**にする。

## Primary Wave Queue
- [x] W0 環境整備: playwright-core + 実機Chrome（iCloud外 `~/.local/share/stage-scan`）で起動確認
- [x] W1 仕様確定: `docs/PROP_RENDER_SCAN_WORKORDER_2026-08-20.md`
- [x] W2 実装（Codex）: `tools/scan-prop-render.mjs` ほか成果物3点
- [x] W3 Claude検証: 自分でスキャナを読み、波A・波Bを1通りずつ実行して挙動を確認
- [x] W4 本走: `--matrix`（波A/B × ja/en × PC/タブレット/スマホ ＝12通り × 32ショー）
- [x] W5 検出の仕分け: コードのバグ / データの指摘 / 誤検出 に分ける（**直さない。報告のみ**）
- [x] W6 朝のレポート作成（`report-summary.md`）＋ PROJECT_NOTES.md へ追記

## Reserve Queue（W4が早く終わったときだけ・事前承認済みの範囲）
- [x] R1 種を変えた反復（`--seed` を 43,44,45…）で注入パターンを変えた追加周回
- [x] R2 既存 `tools/check-object-on-performer.mjs` を全ショーへ再実行し、
       小道具注入と無関係な既知40件が増減していないか確認
- [x] R3 `cd tests && node --test index.mjs` の全通過を再確認（版上げの巻き添え検査）
- [x] R4 スキャン結果のうち「香盤表の列が多いショー（50シーン）」の生成HTMLを1本だけ保存し、
       朝に目視できるようにする（`sample-print.html`）

## 保護対象（絶対に触らない）
- `stage-sketch.js` ほか本体アプリ一式（未commitの小道具作業が乗っている）
- 対象ショーのJSON全部（`jjk-show/` `show-creation/` `.stage-sketch-mcp/`）
- 同期競合コピー15件（削除も移動もしない）

## 中止条件
- Chrome が起動しなくなった / ディスク空きが 10GB を切った → 中止して部分レポートを出す
- 本体アプリのファイルに差分が出た → 即中止して本人へ報告（このタスクは書き込みをしない）

## 完了記録（2026-08-20 04:45・このセッション）

W2〜W6・R1〜R4を完了。結果は `REPORT_2026-08-20.md`。
**致命0 / 描画エラー0 / 香盤表の異常0**（12通り×32ショー＋seed43/44/45の反復3周）。
保護対象39ファイルはSHA-256照合で不一致0（baselineは版上げに伴い `baseline-sha256-v263.txt` へ更新）。
本人判断待ち: `継ぎ目の庭` r1 の許可リスト外6件（退行ではない）。

**注記**: 04:20頃から別セッションが同じ予備キューを並行実行している（`night-loop.sh`・`seeds/`）。
このループは停止していない（07:00に自動終了）。詳細は REPORT_2026-08-20.md 末尾。
