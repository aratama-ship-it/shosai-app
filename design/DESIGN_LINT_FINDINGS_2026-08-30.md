# design-lint 指摘（2026-08-30）— 未対応分の申し送り

`web-projects/design-lint/` で計測した結果の記録。**次にこのアプリを触るときに、ついでに直す**想定。
急いで直す必要はない（致命的な不具合ではなく、主な利用者は本人）。

## 計測条件

- URL: `https://aratama-ship-it.github.io/shosai-app/`（初回訪問時の画面）
- ビューポート: 390×844
- ★**まっさらなブラウザで最初に出る画面**を測っている。ログイン後や別画面は含まない。
- 再計測: `~/.venvs/design-lint/bin/python "…/web-projects/design-lint/design_lint.py" "<URL>"`

## 対応済み

| 項目 | 内容 | 状態 |
|---|---|---|
| **M1** `prefers-reduced-motion` | 動きのある35要素すべてが低モーション設定でも変化なしだった | ✅ **2026-08-30に修正済み**。`style.css` 末尾に全称セレクタの `@media (prefers-reduced-motion: reduce)` を追加し、`style.css?v=220→221` へ更新。ローカル計測で 0/35 → **35/35** を確認 |

## 未対応（次に触るときに）

計測時点で NG 145件。ただし**145個の別々の不備ではなく、共通の値の使い回し**なので、
CSS変数を数個直せば大半が動く。優先順位は下の順。

### 1. C1 コントラスト比 — 41件（本人の可読性に直結するので最優先）

同じ文字色の使い回しで、上位は次のとおり。

| 文字色 | 件数 | 例 |
|---|---|---|
| `#555049` | 8件 | サイドバーのボタンラベル |
| `#48423B` | 5件 | — |
| `#A84B26` | 3件 | 「STAGE IMAGE STUDY」「Beta」 |
| `#5E5850` / `#59544D` | 各3件 | — |

- 背景はダークテーマの `#191512`。最悪 **1.73:1**（基準は本文4.5:1）。
- **直し方の前例**: 2026-08-11の監査で、このアプリは `--rust-ink` を新設して小見出し4クラスへ当て、
  3.44→5.59 に改善している（`skill-library/UI_AUDIT_2026-08-11.md`）。**同じ手が使える。**
- ★**ブランド色そのもの（`--accent` 等）は変えない。** 小さい文字用の濃色を別変数で持つ。
  暗いUIなので「明るくする」方向（明るいUIなら濃くする）。

### 2. U1 タップ対象 44px未満 — 108件（うち効くのは20件ほど）

高さの分布に固まりがある。**全部を44pxにする必要はない。**

| 高さ | 件数 | 判断 |
|---|---|---|
| 38px | 35件 | 幅は十分（例: `#stage-export` は 343×38px）。実用上さほど困らない。**急がない** |
| 36px | 7件 | 同上 |
| 27px / 26px / 22px / 12px | 計37件 | **ここが本命。** 押し間違いが実際に起きる大きさ |

- 12pxのものが8件ある。まずここから。
- 前例: 2026-08-11に `.db-shelf-tab` を 34→44px にした際、
  「下の演出の型カード(44px)と高さが揃い、面の並びも整う」という理由で44pxを採用している。
  **数値合わせではなく、隣接要素と揃うかで決める。**

### 3. T1 行送り 7件 / T4 書体3種類 1件 — 軽微

- 書体は1〜2種類が基準（§12-18）。3種類使われている。

## 背景（なぜこうなったか）

2026-08-11の監査時点では **Phase 0 プロトタイプで39要素**、指摘は解消済みだった。
2026-08-30時点では **v0.3.4 Beta で144要素**、「STAGE IMAGE STUDY」等の機能が加わっている。
**18日間で追加されたUIが基準を外れた**もので、以前の修正が壊れたわけではない。

→ 定期計測に載せて、溜まる前に気づける状態にするのが本筋（別途タスク化）。
