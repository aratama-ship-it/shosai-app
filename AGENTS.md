# 舞台スケッチの既存ユーザー保護（本人指示・2026-09-13）

この規則は舞台スケッチだけに適用し、同居する制作の書斎や他アプリへ一般化しない。
舞台スケッチの変更を始める前に **[BETA_UPDATE_SAFETY.md](BETA_UPDATE_SAFETY.md) を必ず読む**。

**push して公開する前には [PUBLISH_RULES.md](PUBLISH_RULES.md) を読む**（本人決定・2026-09-14「案C」）。
公開に `rebase` を使わないこと、ファイル数が多い公開では先に `bash tools/pre-publish-check.sh` を
通すことを定めた手順の正本。この文書の既存ユーザー保護の規則を置き換えるものではなく、その下で守る公開手順。

- **実装前**に既存機能・既存プロジェクトへの影響を確認する。削除、非表示、無効化、代替UIへの移行、
  保存形式・読込・自動保存・音楽・共有・更新処理の変更で継続利用を損なう可能性があれば、本人へ明示的に警告する。
  不明も警告対象。該当変更のベータ向け統合・反映を止め、防止策を隔離した候補で作り検証する。
- **ベータ更新のたびに**、リスク検出の有無にかかわらず互換性確認の警告を本人へ表示する。
  ツール出力だけで済ませず、影響対象・対策・検証結果・未確認事項を会話にも示す。
- 配布前には、確定した配布候補全体を対象に `python3 tools/check-beta-update-safety.py --candidate <配布候補ディレクトリ> --review <確認記録.json>` を実行する。
  非ゼロ終了・証拠不足・未確認項目があればベータ更新を中止する。迂回や記録の自動承認をしない。
- 直接の Wrangler、API、Workerだけの更新、asset差し替え、別フォルダの配布スクリプト、緊急修正にも同じ規則を適用する。
  配布候補には実際に反映するWorker・設定・assets・移行コード等を含め、送信物との一致を確認する。
- 警告への了承だけで破壊的変更を通さない。防止策と回帰・復旧検証が揃ってから反映する。
  公開許可は別途、既存の本人承認範囲に従う。無関係な安全な作業は継続してよい。

詳細な対象・検証・記録形式・限界は上記文書を正本とする。規則を弱めてチェックを通さない。
