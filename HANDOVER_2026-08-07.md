# 舞台スケッチ 引き継ぎ資料（2026-08-07）

このファイルだけ読めば、別のマシン・別のエージェントが続きを引き取れるように書いてある。
対象は `claude code files/show-creative-ideas/shosai-app/`（iCloud上の共有ワークスペース）。

---

## ★ 次にやること（2026-08-07 夕方時点）

優先順。どちらも本人の確認が要る（無人で残っている作業は無い）。

### ①（解決済み）v=195 のPages公開

一度 GitHub側のOIDC障害（`Failed to get ID Token`）で失敗・再実行も詰まったが、
次のコミットのプッシュで走った新しいデプロイが成功。**公開サイトが `v=195` /
SW `v25` を配信していることを curl で確認済み**（2026-08-07 15:20頃）。
今後デプロイが失敗した場合の対処メモ: 同種の失敗は一時障害のことが多い。
`gh run rerun <run-id> --failed` か、次のプッシュで新しいランを走らせれば直る。
続くときは https://www.githubstatus.com を確認。

### ② iPadでの本人確認

ホーム画面のアプリを完全に終了して開き直してもらう（更新の取り込みに2回必要なことがある）。

- プレゼン中に右上の「見る位置の小図」が**出ない**こと（今回の変更点）
- プレゼンの説明の文字サイズ 小/中/大 の見え方（設定→プレゼンの説明。実機の距離感での
  視認性はまだ本人が確認していない）

### ③ 区切りがついたら新セッションへ

このセッションは持ち越し3件＋iPad不具合対応まで完了している。以降の作業は
このファイルを読めば引き継げる。

---

## 0. 最初に確認すること

作業を始める前に、フォルダが読めるか必ず確かめる。

```bash
ls "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/show-creative-ideas/shosai-app/"
```

`Operation not permitted` が出る場合、このMacのサンドボックスがiCloudフォルダへの許可を
失っている（2026-08-07のセッションで実際に発生した）。**セッションを開き直すと戻る。**
その状態では既存ファイルの読み書きも `node` の実行もできないので、着手しない。

**後始末**: 権限確認のため `tests/.access-probe`（空ファイル）を作ってしまった。
→ **2026-08-07 午後のセッションで削除済み。**

---

## 1. いまのアプリの状態

**完成して動いている。壊れていない。** 2026-08-07 のセッションで下記まで実装・検証済み。

| ファイル | 版 |
|---|---|
| `stage-sketch.js` | `?v=195` |
| `style.css` | `?v=147` |
| Service Worker | `stage-sketch-pwa-v25` |
| テスト | 89件すべて通過 |

このあと夕方までに入った変更（各コミットに詳細あり）:

- **プレゼンの疑似全画面**（`c7dbb5c`→`3b39647`）: iPadのホーム画面PWAでは全画面APIが
  使えず無反応だった。フォールバックで疑似全画面へ。左右タップでシーン送り、✕/Escで終了。
- **iPad横向きの上部欠け修正**（`cd04ca4`）: タブレット盤面の中央寄せクリップをやめ上詰めに。
  16:9幅の高さ予算を 144px→224px（シーンの説明行の分）。
- **プレゼン中は見る位置の小図を出さない**（`d60ae6b`）: 本人指定。編集画面と書き出しでは出る。

（v=193 / v=145 / v22 は、下記「3. 持ち越し」3件を 2026-08-07 午後に実装した際の更新）

**さらに v=194 / v=146 / SW v23（テスト88件）**: iPadのホーム画面PWAでプレゼンが
無反応だった件の修正。standaloneでは全画面APIが使えないため、`requestFullscreen` が
無い・拒否された環境では**疑似全画面**（`enterPseudoPresentation()`、bodyに
`.stage-pseudo-present`、canvasをfixed+object-fit:containで全面表示）に入る。
左右タップ（`#stage-present-prev/next` → `stepScene(±1)`）でシーン送り、✕とEscで終了。
タップゾーンがcanvasを覆うので、プレゼン中の編集タッチは自動的に効かない（意図した仕様）。
テストは `tests/stage-present-fallback.test.mjs`。GitHub Pagesへ公開済み。

版番号は `stage.html` / `index.html` / `stage-sw.js` の3か所と、
`tests/stage-pwa.test.mjs` の中の正規表現が**必ず揃っている必要がある**（テストが揃いを検査する）。
JSやCSSを変えたら4か所すべて上げること。

### 確認のしかた

```bash
cd "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/show-creative-ideas/shosai-app" && node --test tests/*.test.mjs
```

画面の確認は、ローカルの静的ファイルをブラウザで開く。**開いたら ⌘⇧R で強制リロード**
（版番号のクエリだけでは古いJSが残ることがある）。

```bash
open "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/show-creative-ideas/shosai-app/stage.html"
```

---

## 2. このセッションで入れた変更（すべて検証済み）

### 2-1. シーンの説明を正面図の上に表示・その場で編集

- `stage.html`: `#stage-front-cell` の中、キャンバスより前に `#stage-scene-desc` を追加。
  ラベル「シーンの説明」＋枠を持たない `textarea#stage-scene-desc-text`（200字）。
- `stage-sketch.js`: `syncSceneDesc()` / `growSceneDesc()` を追加。描画のたびに同期する。
  シーン一覧側のメモ欄と**双方向**で同期し、打っている最中の欄には書き戻さない
  （書き戻すと変換中の文字が飛び、カーソルが末尾へ跳ねる）。
  対になる一覧側の欄には、開いているのが現在の場面のときだけ `id="stage-scene-note-input"` が付く。
- 高さは内容に合わせて 19px〜96px。セクション（章の見出し）行では出さない。
- `stage-i18n.js`: `"シーンの説明": "Scene description"` を追加。

### 2-2. 平面図の重ね順を直した（報告された不具合）

**症状**: 盆や台の上に立っている人が、平面図で装置の下に隠れて見えなかった。

**原因**: 平面図だけ `scene.pieces` の配列順そのままで描いていた。あとから足した装置が
上に来ていた。真上から見た図なのに関係が逆だった。

**直し方**: 床から高い順（`base + pieceTopLocal()`）に重ね、**演者は必ず最後**に描く。
誰がどこに立っているかが平面図の一番の用なので、装置に埋もれないことを優先した。

```js
const planLayer = (p) => (p.type === "performer" ? 1 : 0);
const topH = (p) => finite(p.base, 0) + pieceTopLocal(p);
solid.slice().sort((a, b) => (planLayer(a) - planLayer(b)) || (topH(a) - topH(b)))
```

高さ2.5mの壁を演者と同じ位置・配列順であとに置いても、演者が隠れないことを実機で確認済み。

### 2-3. 「舞台上／舞台裏」で位置がリセットされる不具合（報告された不具合）

**症状**: 装置を舞台裏へ下げてから舞台上へ戻すと、既定位置へ飛んで配置が壊れた。

**原因**: 下げるときに駒を丸ごと捨て、戻すときに `placeSetPiece()` の既定位置へ置き直していた。

**直し方**: シーンごとに「置き場所の控え」`scene.stashed`（`setId` または `castId` を鍵）を持ち、
下げるときに控え、戻すときにそこから起こす。

対象は**4か所すべて**（同じ不具合が残っていたので同時に直した）:

| 場所 | 関数 |
|---|---|
| 舞台セットのトグル | `toggleSetOnStage()` |
| 演者のトグル | `toggleCastOnStage()` |
| 照明の組のON/OFF | `toggleLightGroup()`（控えが無ければ `item.preset` へ） |
| Deleteキーで消す | `removeSelected()` |

**設計上の決め事（変えるときは理由を持って）**

- 控えるのは「登録から引き直せないものだけ」＝ `STASH_KEYS`
  （`u,v,size,facing,pose,poleSide,poleH,trapMode,glow,beam,route,locked,name,type`）。
  **寸法・色は舞台セットの登録側**が持ち、**床からの高さ・支え**は置き場所から毎回引き直す。
  丸ごと控えるとシーンの数だけ同じ値の写しが溜まる。1件あたり約650B→約300Bになった。
  副作用として、舞台裏の間に装置の寸法を変えると、出し直したとき新しい寸法になる（正しい挙動）。
- `checkpoint()` は**必ず控えを書く前**に呼ぶ。順が逆だと「一つ戻す」で控えごと戻せない。
- 登録（舞台セット・名簿）を外したら、その控えも捨てる。
- 行き場のない控えは読み込み時に `sweepStashes()` が掃く（`sweepPhotos()` の隣で呼ぶ）。

**「一つ戻す」は全ケースで正しく動くことを実機確認済み**（下げる／戻す／やり直す／
動かしてから下げる／登録ごと外してから戻す／照明の組8灯）。履歴は state 全体のJSON写しで、
駒のid採番カウンタは state の外にあるので、undoで番号が巻き戻って衝突することもない。

**★重要（本人確定）**: この控えの仕組みは**画面に一切示さず、黙って効かせる**。
一覧に「控えあり」の印を出すことは検討したうえで不要と判断された。**再提案しないこと。**

### 2-4. プレゼンモードにシーンの説明

プレゼンは canvas 要素だけを全画面にする作りなので、HTMLの欄は出てこない。
`drawSceneCaption()` で**絵の中に字として描き込む**。

- 描くのは**全画面の正面図のときだけ**（`presenting && target === ctx && !L.plan`）。
  編集中の画面と、書き出す画像には出さない（画像に字が焼き付くのを避ける）。
- `presenting` は `fullscreenchange` で切り替え、変化時に `render()` する。
- 見出しは**シーン名**にした。「シーンの説明」は画面の道具の名前で、人に見せる絵の中では
  意味を持たない。見ている側が知りたいのは「いまどの場面か」。
- 絵の下側に、下だけを暗く落とす帯（絵全体は曇らせない）。字には影を一枚敷く。
- `wrapCaption()` で幅に合わせて折り返す。日本語は一文字ずつ、英語は語の切れ目。上限5行。
- **落とし穴**: 帯は `setTransform(1,0,0,1,0,0)` で拡大・移動から外している。
  外さないと、二本指で寄せたときに字だけ巨大化して枠から出る（実際に一度そうなった）。

### 2-5. 印刷用ページにシーンの説明

`openPrintPage()`。説明は元からあったが、絵の下に無印で置かれていた。
**枠付きの見出し「シーンの説明」を付け、絵より前**へ移した。
何が起きる場面かを読んでから図を見る順になる。

改ページへの影響が無いことを実測で確認済み（A4縦の使える高さ約1047pxに対し、
最も長いシーンでも368px。印刷CSSは元から `page-break-after: always` で1シーン1ページ）。

### 2-6. テスト

`tests/stage-scene-desc-stash.test.mjs`（17件）を新規追加。既存と同じく**ソースの
パターン検査**の形。上の「設計上の決め事」を固定するのが目的なので、実装を変えるときは
このテストが何を守っているかを読んでから直すこと。

---

## 3. 持ち越し（仕様は本人と確定済み。判断を蒸し返さず実装するだけ）

**→ 3件とも 2026-08-07 午後のセッションで実装済み**（Codexに実装させ、Claudeが検証。
テスト84件通過、設定画面・印刷ページ・プレゼン描画をブラウザで実機確認済み）。
以下は実装時の仕様の記録として残す。

- 設定の保存先: `prefs.presentCaption`（出す/出さない、既定ON）と
  `prefs.presentCaptionSize`（`"small"|"medium"|"large"`、既定 `"medium"`）
- 印刷ページの切替は生成HTML内の `.print-toggle`＋インラインscriptで完結
  （2シーン時は `body.two-up`、2件ごとに `.pb` で改ページ。保存なし）

権限障害で着手できなかった3件。

### 3-1. プレゼンの説明を設定から変更可能にする

- 設定（環境設定）に2項目を足す: **出す/出さない** と **文字サイズ（小・中・大）**
- 段階は **小=15px / 中=19px（既定） / 大=25px**。既定は本人が選択済み。
- 現在は `drawSceneCaption()` の中で `const size = 25` の決め打ち。ここを設定値から引く。
- 見出し（シーン名）の `kick` は**本文の約0.62倍**にすると、いまの比率が保てる。
- 参考実測（この長さの説明文で）:

  | サイズ | 本文 | 帯の高さ | 絵に占める割合 |
  |---|---|---|---|
  | 25px | 2行 | 174px | 24% |
  | 19px | 2行 | 154px前後 | 21% |
  | 15px | 1行 | 114px | 16% |

### 3-2. 印刷用ページに「1シーン/ページ ⇔ 2シーン/ページ」の切り替え

- 置き場所は**生成した印刷ページの中**（本人選択）。印刷には出さない小さな切り替えにする。
- 2シーン/ページのときは絵を縮め、`page-break-after` を2件ごとにする。
- 選択はそのページ内だけで完結させ、**本体のUIと保存は増やさない**。
- 背景: いまは1シーン1ページで、ページの約1/3しか使っていない。稽古場での書き込み余白として
  意図的でもあるので、既定は現状（1シーン/ページ）のままにして、選べるようにする。

### 3-3. テスト1件の追加

`tests/stage-scene-desc-stash.test.mjs` に、プレゼンの見出しがシーン名であることを固定する。

```js
test("プレゼンの見出しはシーン名にする（画面の道具の名前は出さない）", () => {
  const fn = stageSource.slice(stageSource.indexOf("function drawSceneCaption"));
  const body = fn.slice(0, fn.indexOf("\n  }\n"));
  assert.match(body, /const title = \(scene\.title \|\| ""\)\.trim\(\)/);
});
```

83件→84件になる。

---

## 4. 触った場所の索引

| 何 | どこ |
|---|---|
| シーンの説明の欄（HTML） | `stage.html` の `#stage-front-cell` 内 |
| 同（見た目） | `style.css` の `.stage-scene-desc*` |
| 同（同期） | `stage-sketch.js` `syncSceneDesc()` / `growSceneDesc()` |
| プレゼンの帯 | `stage-sketch.js` `drawSceneCaption()` / `wrapCaption()` / `presenting` |
| 平面の重ね順 | `stage-sketch.js` `drawStage()` 内の `planLayer` / `topH` |
| 位置の控え | `stage-sketch.js` `STASH_KEYS` / `normalizeStash()` / `stashPiece()` / `restoreStashed()` / `sweepStashes()` |
| 印刷用ページ | `stage-sketch.js` `openPrintPage()` |
| テスト | `tests/stage-scene-desc-stash.test.mjs` |

---

## 5. このプロジェクトで気をつけること

- **パスに空白がある。** シェルでは引用符を徹底する。
- **iCloudの共有ワークスペース。** Codexや別PCが同じファイルを触っている前提で、
  編集前に読み直す。勝手に巻き戻さない。同期競合コピー（「〇〇 2.md」）は削除せず本人に報告。
- **削除・移動を伴う一括処理は、先にスナップショットを取ってから。**
- ブラウザでの検証は `localStorage` の `shosai-stage-sketch-v1` を直接読むと確実
  （保存はデバウンスされるので、読む前に0.6〜0.9秒待つ）。
