# 舞台スケッチ リアルタイム共有セッション 設計書

- 作成: 2026-08-20（Claude）
- 状態: **設計のみ。未実装。本人の承認前**
- 関連資料: `SYNC_DESIGN.md`（Supabaseによる保存・共同制作の設計。本書とは別機構）
- 対象コード: `shosai-app/`（このファイルと同じディレクトリ）

## 目的と要件（2026-08-20 本人ヒアリング）

ZOOM会議等の打ち合わせ中に、複数人が同じ舞台スケッチをリアルタイムに見て・触って、
演出の認識をシェアできるようにする。

- 人数: **最低3人、できれば20人まで**
- 役割: **ホスト（メインの操作者）1人＝全機能**。
  **ゲスト＝簡易操作のみ**（矢印を書く、演者を動かす、道具を動かす程度で十分）
  - ※「援助動かす」は音声入力の揺れと判断し「道具（プロップ）を動かす」と解釈した。
    実装前に本人へ要確認。
- **Supabaseは使わない**（本人の無料枠を他プロジェクトで消費済みのため。
  どうしても必要になったら再検討、が本人の意向）
- 同時編集（Figma的なCRDT共同編集）はやらない。ホストが正、ゲストは限定操作。

## 技術方式: Cloudflare Durable Objects（既存Workerに同居）

shosai-app はすでに Cloudflare Workers（`wrangler.toml` + `worker.js`、Basic認証つき）で
配信されている。同じWorkerに Durable Object（以下DO）を1クラス追加し、
「1セッション＝1 DOインスタンス」のWebSocketルームにする。

### 無料枠の裏取り（2026-08-20 Cloudflare公式docsで確認）

- DOは**無料プランで利用可能**（SQLiteバックエンドのクラスのみ。新規クラスは既定でSQLite）
- 無料枠: **10万リクエスト/日、313,000 GB秒/日**（毎日00:00 UTCリセット）
- WebSocket受信メッセージは **20:1換算**（受信100メッセージ＝課金上5リクエスト）
- **WebSocket Hibernation API** を使えばアイドル中のduration課金なし
- 出典: https://developers.cloudflare.com/durable-objects/platform/pricing

概算: 20人×2時間、平均5操作/秒（かなり多め）でも受信約36,000メッセージ
＝換算1,800リクエスト。無料枠の2%未満。**この用途では実質無料**。

### なぜSupabase Realtimeではないか

- 本人の無料枠事情（上記）に加え、書斎アプリの「外部SDK持ち込み禁止」制約下では
  RealtimeのPhoenixチャネルプロトコルを素のWebSocketで自前実装する必要があり、
  DOの素のWebSocketより手間が大きい。
- セッションは揮発でよく（保存はローカルが正本のまま）、DBを持つ理由がない。

## アーキテクチャ

```
ホスト                     Cloudflare Worker + DO                ゲスト×N (〜19)
┌─────────┐   WebSocket   ┌──────────────────┐   WebSocket   ┌─────────┐
│ 舞台スケッチ │◀──────────▶│ SessionRoom (DO)  │◀──────────▶│ 舞台スケッチ │
│ (全機能)   │  snapshot/op │ ・最新snapshot保持 │  snapshot/op │ (限定操作) │
└─────────┘              │ ・opを全員へ中継    │              └─────────┘
                          │ ・ロール別許可判定  │
                          └──────────────────┘
```

- **ローカルファーストは崩さない。** セッションは揮発。DOはセッション中だけ
  最新snapshotをメモリ（＋再接続用にDO storage）に持ち、セッション終了で消える。
  保存は従来どおりホストのlocalStorage／ファイル書き出し。
- 音声はZoom側に任せる。このシステムは画面状態の共有だけを担う。
- 参加者は各自のブラウザで開くので、**カメラアングル（俯瞰・一人称等）は各自が自由に選べる**。
  画面共有と違い「自分の見たい角度で見ながら議論できる」のが本方式の核となる利点。

### セッションの流れ

1. ホストが舞台スケッチ内で「セッション開始」→ Workerが乱数ルームID＋ホストトークンを発行
2. 招待URL（`stage.html#session=ROOMID`）をZoomチャットで共有。
   サイト自体は既存Basic認証が関所（ゲストは既存の `GUEST_USER/GUEST_PASS` を使う）
3. 参加者がURLを開くとWebSocket接続 → DOが最新snapshot（
   `SHOSAI_STAGE_PROJECT_IO.exportDocument()` のJSON）を送る → 以後は差分opを中継
4. ホストが切断してもルームは一定時間（例: 10分）生存し再接続を待つ。
   タイムアウトでルーム消滅

### プロトコル（op方式）

| 送信者 | op種別 | 内容 |
|---|---|---|
| ホスト | `snapshot` | プロジェクト全体のJSON（開始時・大規模変更時・定期チェックポイント） |
| ホスト | `patch` | 状態差分（当面は「変更のたびsnapshot再送」でも20人規模なら帯域的に成立。差分化は最適化として後回し可） |
| ゲスト | `piece.move` | 演者・道具の位置移動のみ（id＋座標。サイズ・削除・追加は不可） |
| ゲスト | `annotation.add` / `annotation.clear` | 矢印・フリーハンド線の追加と自分の分の消去 |
| 全員 | `pointer` | レーザーポインタ（名前＋色つきの光点。「ここ」を指せる。認識シェアの本命機能） |
| 全員 | `presence` | 入退室・名前・色 |

- **許可判定はDO側（サーバ側）で行う。** ゲストのクライアントUIを絞るだけでは不十分で、
  DOがロールを見てホスト専用opを拒否する。
- 競合はオブジェクト単位のlast-write-wins。ゲストは限定opしか打てないため、
  CRDTなしで実用上破綻しない。構造的な編集（シーン追加・機構操作等）はホストのみ。
- ゲストの注釈・移動はホストの画面にも反映され、ホストが保存すれば成果物に残る
  （残したくない場合に備え「ゲスト注釈を一括消去」をホストに用意する）。

## 実装ステップ（Codex発注の分割単位）

**B0: 閲覧中継＋レーザーポインタ**（ゲストは見る＋指すだけ）
- `wrangler.toml` に `[[durable_objects.bindings]]` と `[[migrations]]`（SQLiteクラス）追加
- `worker.js` にルート `/session/*` を追加（Basic認証通過後にDOへ委譲）
- 新規 `stage-session.js`（クライアント）: 接続・snapshot適用・pointer送受・参加者リスト表示
- ホスト側: 状態変更フックからsnapshot送信（既存の保存トリガに相乗り）

**B1: ゲストの限定操作**
- `piece.move` / `annotation.add` のop化とサーバ側ロール判定
- ゲスト用UI制限（ツールバーを閲覧＋矢印＋移動だけに縮退）

**B2（様子見・任意）**: 差分patch化、セッション録画（opログ再生）等

- 見積もり（推測と明示）: B0が実装2〜4時間規模＋実機検証、B1が同程度。Codex向き。
- 検証: ブラウザ2窓＋別端末で、snapshot追従・ロール拒否・再接続を確認してから報告する。
- 注意: `stage.html` は生成物。クライアント変更は `index.html` 側に入れて
  `build_stage.py` を回す（メモリ `shosai-app-two-html-entries`）。
  JS変更時は `?v=` と `stage-sw.js` の `CACHE_NAME` を上げる（`shosai-app-cache-bump-required`）。

## 確定事項（2026-08-20 本人回答）

1. ゲストの移動操作は**演者を動かす（道具も含む）**で確定
2. ゲストの表示名は参加時にその場で入力（アカウント不要）
3. ゲスト注釈はホストが保存すれば成果物に残る。ホストに「ゲスト注釈を一括消去」を用意
   （本人から異論なし＝既定案を採用）
4. **B1まで揃えてからチームで試用**で確定

## 実装上の確定仕様（コード調査 2026-08-20）

- 状態転送は `SHOSAI_STAGE_PROJECT_IO.exportDocument(state.project)`（version 4、
  カスタム会場を同梱するのでゲスト側でも正しく描ける）を使う。
  受信側は `prepareImportDocument` → state.project 差し替え → 再描画。
- ホストの変更検知は `persistSoon()`（stage-sketch.js 4346行、全保存が通る単一路）に
  フックを1つ追加するだけでよい。
- ゲストの限定操作は「差分検出方式」: ゲスト側も既存UIで操作させ、persistSoonフックで
  直前の受信docと比較し、**新規矢印**と**駒(u/v/base)の移動**だけをopとして送る。
  それ以外のローカル変更は送らず、次のホストdocで上書きされる（ホストが正）。
  これにより stage-sketch.js 内部への侵襲を最小化できる。
- `normalizeArrow`（3110行付近）は未知フィールドを落とすため、ゲスト注釈タグ
  `guest: true` を保持する1行拡張が必要。
- ゲスト参加時は既存の `shelveCurrent()` で自分の作業をショー一覧へ退避してから
  セッションのdocを開く（自分の作品が消えない）。
- 発注書: `docs/REALTIME_SESSION_WORKORDER_2026-08-20.md`

## 使い方（2026-08-20 本番デプロイ済み: https://shosai-app.juggler-arata.workers.dev ）

1. **ホスト**: `/stage.html` を本人用ID/パスで開く → 「リアルタイム共有（会議用）」パネル
   → 表示名を入れて「セッションを開始」→ 招待URLを「コピー」
2. 招待URLと**ゲスト用のID/パス（Cloudflare SecretsのGUEST_USER/GUEST_PASS）**を
   Zoomチャット等で共有する（URLだけではBasic認証で入れない）
3. **ゲスト**: URLを開く → Basic認証 → 表示名を入力して参加。
   参加時に自分の作業は自動でショー一覧へ退避される
4. ゲストができること: 演者・道具のドラッグ移動と矢印（全員へ反映）、
   レーザーポインタ（平面ビュー同士でのみ見える）。カメラアングルは各自自由
5. **シーンはホスト優先**（2026-08-20 本人指示で追加）: ゲストはシーンを切り替えられず、
   常にホストの表示中シーンに追従する。実装は `openScene()` 冒頭のガード
   （stage-sketch.js）＋前/次ボタン非表示＋シーン一覧の操作無効（style.css）。
   ホストからの同期は `applyDocumentString` が activeSceneId を直接差し替えるため影響なし
6. **「いま操作中: ◯◯」バナー**（2026-08-20 本人提案で追加）: 誰かが操作すると
   他の全員の画面上部中央に名前・色つきで約3秒表示される。同時操作の衝突を
   互いの操作を見せることで避ける仕組み（プロトコル: `{t:"activity"}` を中継）
7. ホストの保存は従来どおり（ゲストの矢印も残る。「ゲスト注釈を一括消去」あり）。
   セッションは全員退出の10分後に消える。上限20人
8. 画面が古い場合は再読み込み（PWAキャッシュ更新は `stage-sw.js` の版で管理）

## 不具合修正の記録

- **2026-08-20 ゲストの照明移動が反映されない**: 照明も他の駒と同じ `piece.move` で
  同期される設計自体は正しかったが、**ホストのdoc配信とゲストの操作が重なると、
  ゲストの未送信変更がdoc適用で消えてから差分検出が走り、opが送られない**競合窓があった。
  修正: `applyRemoteDocument` の冒頭で、doc適用**前**に `sendGuestDifferences()` を
  呼んで未送信分を先に流す（stage-session.js）。ゲスト側で時間差0.55〜0.75秒の
  連続3操作がすべて生き残ることを実機確認済み。

## ローカル検証の作法（2026-08-20 実機で確認した落とし穴）

- `[assets] directory = "."` のため、素の `npx wrangler dev` は**自分が書くDO状態ファイル
  （`.wrangler/state`）をアセット変更として検知し、無限リロードでWebSocketが数秒おきに
  全滅する**（症状: welcome だけ届き、中継系が全部沈黙する）。
- 正しい起動方法: プロジェクトを `/tmp` 等へ複製し、
  `npx wrangler dev --port 8788 --persist-to /tmp/wrangler-persist` のように
  **状態の保存先をプロジェクト外へ出す**。iCloud上で直接devを走らせない
  （iCloud同期の書き込みでも同じリロードが起きる）。
- 本番（`wrangler deploy`）には無関係。dev限定の問題。
- サーバ側の通しテスト: `/tmp/ws-session-test.mjs`（Claude作成、13項目）を
  `node` で実行。恒久保存するなら `tests/` へ移す。
