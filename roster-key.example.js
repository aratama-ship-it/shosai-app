/* 名簿（アーティスト）の合言葉を置く場所の雛形。
 *
 * 使い方:
 *   1. このファイルを roster-key.local.js という名前でコピーする。
 *   2. 下の "" に、スカウティングレポートと同じ合言葉を書く。
 *   3. wrangler deploy する。roster-key.local.js は .gitignore 済みなので
 *      公開リポジトリには入らず、Cloudflareの配信物にだけ載る。
 *
 * なぜこの形か（2026-08-20 決定）:
 *
 * サイト全体が worker.js のBasic認証で守られるようになったので、書斎を開けた時点で
 * 本人かチームのゲストである。そこで名簿の合言葉を毎回聞くのはもう二度手間になった。
 *
 * ただし合言葉そのものを消すことはできない。名簿の実体 data.enc は
 * scouting-report-app の GitHub Pages にあり、あれは Basic認証の外＝誰でも取得できる。
 * あのファイルを守っているのは暗号化だけなので、暗号化はこのまま残す。
 *
 * そして shosai-app リポジトリは公開なので、合言葉を追跡対象のファイルへ書くと
 * GitHub上で読めてしまい、data.enc を誰でも復号できることになる。
 * だから「gitには入れない・Cloudflareへは載せる」このファイルに分ける。
 *
 * このファイルが無ければ、これまでどおり合言葉の入力欄が出る（壊れない）。
 */
const SHOSAI_ROSTER_KEY = "";
